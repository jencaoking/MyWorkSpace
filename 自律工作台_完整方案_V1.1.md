# 自律工作台 · 完整技术方案

> **项目定位**：单人专属的原生Android自律管理工具，云端存储、离线可用、数据自动同步  
> **目标用户**：仅开发者本人  
> **文档版本**：V1.1  
> **更新日期**：2026-07-29  
> **V1.1变更**：后端由Ktor(Kotlin)改为PHP；新增免费/低价云主机选型；域名由自备解决

---

## 目录

- [一、项目概述](#一项目概述)
- [二、功能需求总览](#二功能需求总览)
- [三、技术选型](#三技术选型)
- [四、Android端架构设计](#四android端架构设计)
- [五、后端架构设计（PHP）](#五后端架构设计php)
- [六、数据库设计](#六数据库设计)
- [七、接口文档草案](#七接口文档草案)
- [八、离线同步方案](#八离线同步方案)
- [九、通知提醒方案](#九通知提醒方案)
- [十、第三方API接入](#十三方api接入)
- [十一、页面清单与导航结构](#十一页面清单与导航结构)
- [十二、主题与UI规范](#十二主题与ui规范)
- [十三、开发阶段计划](#十三开发阶段计划)
- [十四、风险与应对](#十四风险与应对)
- [十五、不做的事（范围边界）](#十五不做的事范围边界)
- [附录A：Git分支策略](#附录agit分支策略)
- [附录B：服务器选型清单](#附录b服务器选型清单)
- [附录C：关键决策记录](#附录c关键决策记录)
- [附录D：PHP后端目录结构与示例](#附录dphp后端目录结构与示例)

---

## 一、项目概述

### 1.1 产品定位

一款面向**个人**的自律工作台App，整合任务打卡、运动记录、英语学习、影音书籍管理、健康就诊、记账、笔记灵感等模块，帮助用户在一个App内管理所有自律相关的数据和习惯。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| 单人专属 | 无登录注册，设备ID即用户ID |
| 云端优先 | 所有数据存云端，本地为缓存 |
| 离线可用 | 无网络时正常浏览和操作，联网自动同步 |
| 模块化 | 各功能模块独立，可开关显示 |
| 轻量克制 | 不做社区、不做分享、不做复杂配置 |

### 1.3 技术栈一览

| 层级 | 技术 | 说明 |
|------|------|------|
| 客户端 | Kotlin + Android SDK | 原生Android |
| 架构 | MVVM + Repository | 官方推荐架构 |
| DI | Hilt | 依赖注入 |
| 异步 | Kotlin Coroutines + Flow | 协程 + 响应式流 |
| 本地DB | Room (SQLite) | 结构化存储 + FTS全文搜索 |
| 网络 | Retrofit + OkHttp | HTTP客户端 |
| 图片 | Coil | Kotlin-first图片加载 |
| 后台 | WorkManager | 可靠的后台同步 |
| 通知 | AlarmManager + NotificationManager | 精确闹钟 + 通知栏 |
| Markdown | Markwon | 编辑 + 渲染 |
| 图表 | MPAndroidChart | 统计图表 |
| **后端** | **PHP 8.2+ (原生 / 轻量框架)** | **零成本、易部署、生态成熟** |
| 后端DB | MySQL 8.0 | 开源免费、资料多、phpMyAdmin好用 |
| Web服务器 | Nginx | 反向代理 + 静态资源 |
| 服务器 | 免费/低价云主机 | 见附录B |
| 域名 | 自备 | 已解决 |

---

## 二、功能需求总览

### 2.1 功能模块矩阵

| 模块 | 功能点 | 优先级 |
|------|--------|--------|
| 任务打卡 | 一次性待办、循环打卡、长期目标 | P0 |
| 任务统计 | 完成率、日历视图、月度图表 | P0 |
| 分类管理 | 自定义板块、自由新增 | P0 |
| 运动记录 | 项目+时长+历史 | P1 |
| 英语学习 | 翻译、单词、录音跟读（外接API） | P1 |
| 影音书籍 | 书单、电影清单(TMDB)、状态标记、评分 | P1 |
| 健康就诊 | 病历、用药、复诊提醒 | P1 |
| 记账 | 收支记录、分类、月度统计 | P1 |
| 天气 | 实时天气展示（外接API） | P2 |
| 笔记系统 | Markdown图文笔记、上传图片 | P0 |
| 全局搜索 | 跨模块全文检索 | P0 |
| 通知提醒 | 任务提醒、复诊闹钟 | P0 |
| 主题切换 | 浅色/深色模式 | P0 |
| 板块开关 | 用户控制首页显示哪些模块 | P1 |

---

## 三、技术选型

### 3.1 Android端技术栈

| 类别 | 库 | 版本 | 用途 |
|------|-----|------|------|
| 语言 | Kotlin | 2.0+ | 主开发语言 |
| UI | ViewBinding + Material3 | latest | 界面构建 |
| 架构 | Lifecycle + ViewModel | 2.8+ | MVVM |
| 响应式 | Flow + StateFlow | Kotlin 2.0 | 数据流 |
| DI | Hilt | 2.52+ | 依赖注入 |
| 本地DB | Room | 2.6+ | ORM + FTS |
| 网络 | Retrofit + OkHttp | 2.11 / 4.12 | HTTP |
| 图片 | Coil | 2.6+ | 图片加载缓存 |
| 后台 | WorkManager | 2.9+ | 后台同步 |
| 通知 | AndroidX Notification | latest | 通知栏 |
| Markdown | Markwon | 4.3+ | MD编辑渲染 |
| 图表 | MPAndroidChart | 3.1+ | 统计图表 |
| 序列化 | kotlinx.serialization | 1.6+ | JSON |
| 崩溃 | Firebase Crashlytics | latest | 崩溃收集 |

### 3.2 后端技术栈（PHP）

| 类别 | 技术 | 用途 |
|------|------|------|
| 语言 | PHP 8.2+ | 服务端逻辑 |
| 框架 | **可选**（原生PHP / Slim / Laravel轻量版） | 路由+中间件 |
| 数据库 | MySQL 8.0 | 主数据库 |
| 数据库驱动 | PDO | 安全查询（防SQL注入） |
| Web服务器 | Nginx | HTTP服务 + HTTPS终止 |
| 进程管理 | PHP-FPM | FastCGI进程管理 |
| 缓存 | APCu / Redis（可选） | 接口缓存加速 |
| API文档 | 手写Markdown（本文档） | 轻量够用 |

> **框架选择建议**：
> - **不想引入框架**：纯PHP写路由分发，一个`index.php`入口 + `api/`目录，最简单
> - **想要规范一点**：Slim 4（微框架，~500KB，学5分钟就能上手）
> - **不推荐Laravel**：对个人项目太重，启动慢、内存占用高

### 3.3 第三方API

| 服务 | 用途 | 说明 |
|------|------|------|
| TMDB | 电影搜索/元数据 | 免费、有中文、海报CDN |
| 和风天气 | 实时天气 | 免费额度够个人用 |
| 有道智云 | 翻译/单词 | 英语模块核心 |

---

## 四、Android端架构设计

### 4.1 整体分层

```
┌──────────────────────────────────────────────┐
│            Presentation Layer                │
│   Activity / Fragment + ViewBinding         │
│   ViewModel + StateFlow                      │
├──────────────────────────────────────────────┤
│              Domain Layer                     │
│   UseCase + Repository Interfaces             │
│   Model (纯Kotlin业务模型)                    │
├──────────────────────────────────────────────┤
│               Data Layer                      │
│  ┌──────────────────┐  ┌──────────────────┐  │
│  │   Local (Room)   │  │  Remote (API)    │  │
│  │  DAO + Entity    │  │ Retrofit Service │  │
│  └──────────────────┘  └──────────────────┘  │
│         │                          │          │
│         └──── Repository Impl ────┘          │
│                    │                         │
│              SyncManager                     │
│         (WorkManager + ConflictResolver)     │
└──────────────────────────────────────────────┘
```

### 4.2 核心设计原则

1. **单向数据流**：UI → ViewModel → Repository → DataSource
2. **单一可信源**：UI永远从Room读，网络只负责更新本地
3. **乐观更新**：用户操作先更新本地DB，再异步同步云端
4. **关注点分离**：每个模块独立Repository、独立UseCase、独立ViewModel

### 4.3 分包结构

```
com.selfdiscipline.app
├── SelfDisciplineApp.kt
│
├── common/
│   ├── base/              # BaseActivity, BaseViewModel, BaseFragment
│   ├── config/            # ApiConfig, Constants, Endpoints
│   ├── extensions/        # Kotlin扩展函数
│   ├── result/            # Result<T>, Loading/Success/Error
│   └── utils/             # DateUtil, NetworkUtil, PermissionUtil
│
├── di/                    # Hilt Modules
│   ├── AppModule.kt
│   ├── DatabaseModule.kt
│   ├── NetworkModule.kt
│   └── RepositoryModule.kt
│
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt
│   │   ├── dao/
│   │   │   ├── TaskDao.kt
│   │   │   ├── CategoryDao.kt
│   │   │   ├── NoteDao.kt
│   │   │   ├── FitnessDao.kt
│   │   │   ├── MediaDao.kt
│   │   │   ├── HealthDao.kt
│   │   │   ├── LedgerDao.kt
│   │   │   └── SearchDao.kt
│   │   └── entity/
│   │       ├── BaseEntity.kt       # 含同步字段的抽象基类
│   │       ├── TaskEntity.kt
│   │       ├── CategoryEntity.kt
│   │       ├── NoteEntity.kt
│   │       ├── FitnessEntity.kt
│   │       ├── MediaEntity.kt
│   │       ├── HealthEntity.kt
│   │       └── LedgerEntity.kt
│   │
│   ├── remote/
│   │   ├── api/
│   │   │   ├── TaskApi.kt
│   │   │   ├── NoteApi.kt
│   │   │   ├── FitnessApi.kt
│   │   │   ├── MediaApi.kt
│   │   │   ├── HealthApi.kt
│   │   │   ├── LedgerApi.kt
│   │   │   ├── WeatherApi.kt
│   │   │   ├── TmdbApi.kt
│   │   │   └── EnglishApi.kt
│   │   ├── dto/
│   │   │   ├── request/           # 请求体DTO
│   │   │   └── response/          # 响应体DTO
│   │   └── interceptor/
│   │       └── DeviceIdInterceptor.kt
│   │
│   ├── repository/
│   │   ├── TaskRepositoryImpl.kt
│   │   ├── NoteRepositoryImpl.kt
│   │   ├── FitnessRepositoryImpl.kt
│   │   ├── MediaRepositoryImpl.kt
│   │   ├── HealthRepositoryImpl.kt
│   │   ├── LedgerRepositoryImpl.kt
│   │   └── SearchRepositoryImpl.kt
│   │
│   └── sync/
│       ├── SyncManager.kt          # 同步调度核心
│       ├── SyncWorker.kt           # WorkManager Worker
│       ├── SyncStatusTracker.kt    # 同步状态追踪
│       └── ConflictResolver.kt     # 冲突解决策略
│
├── domain/
│   ├── model/
│   │   ├── Task.kt
│   │   ├── Category.kt
│   │   ├── Note.kt
│   │   ├── FitnessRecord.kt
│   │   ├── MediaItem.kt
│   │   ├── HealthRecord.kt
│   │   ├── LedgerEntry.kt
│   │   └── SearchResult.kt
│   ├── repository/                 # Repository接口定义
│   │   ├── TaskRepository.kt
│   │   └── ...
│   └── usecase/
│       ├── task/
│       │   ├── CreateTaskUseCase.kt
│       │   ├── CompleteTaskUseCase.kt
│       │   ├── GetTasksUseCase.kt
│       │   └── DeleteTaskUseCase.kt
│       ├── sync/
│       │   └── SyncNowUseCase.kt
│       └── ...
│
├── feature/
│   ├── home/                # 首页
│   │   ├── HomeActivity.kt
│   │   ├── HomeViewModel.kt
│   │   ├── HomeAdapter.kt
│   │   └── module/
│   │       ├── WeatherCard.kt
│   │       ├── TaskOverviewCard.kt
│   │       ├── FitnessCard.kt
│   │       └── ...
│   ├── tasks/               # 任务打卡
│   │   ├── TaskListActivity.kt
│   │   ├── TaskDetailActivity.kt
│   │   ├── TaskEditActivity.kt
│   │   ├── TaskStatsActivity.kt
│   │   └── adapter/
│   ├── categories/          # 分类管理
│   │   └── CategoryManageActivity.kt
│   ├── fitness/             # 运动
│   │   ├── FitnessActivity.kt
│   │   └── FitnessRecordEditActivity.kt
│   ├── english/             # 英语
│   │   ├── EnglishHomeActivity.kt
│   │   ├── TranslateActivity.kt
│   │   ├── WordBookActivity.kt
│   │   └── PronunciationActivity.kt
│   ├── media/               # 影音书籍
│   │   ├── MediaListActivity.kt
│   │   ├── MediaDetailActivity.kt
│   │   ├── MediaSearchActivity.kt  # TMDB搜索
│   │   └── BookListActivity.kt
│   ├── health/              # 健康
│   │   ├── HealthActivity.kt
│   │   ├── VisitRecordEditActivity.kt
│   │   └── MedicationEditActivity.kt
│   ├── ledger/              # 记账
│   │   ├── LedgerActivity.kt
│   │   ├── LedgerEditActivity.kt
│   │   └── LedgerStatsActivity.kt
│   ├── notes/               # 笔记
│   │   ├── NoteListActivity.kt
│   │   ├── NoteEditActivity.kt
│   │   └── NoteViewActivity.kt
│   ├── search/              # 全局搜索
│   │   └── GlobalSearchActivity.kt
│   └── settings/            # 设置
│       ├── SettingsActivity.kt
│       ├── ThemeSettingsActivity.kt
│       └── ModuleToggleActivity.kt
│
├── notification/
│   ├── NotificationScheduler.kt
│   ├── NotificationReceiver.kt
│   └── BootReceiver.kt
│
├── weather/
│   ├── WeatherManager.kt
│   └── WeatherWidget.kt
│
└── ui/
    ├── theme/
    │   ├── LightTheme.kt
    │   ├── DarkTheme.kt
    │   ├── Color.kt
    │   ├── Type.kt
    │   └── ThemeManager.kt
    ├── components/
    │   ├── LoadingView.kt
    │   ├── EmptyView.kt
    │   ├── ConfirmDialog.kt
    │   └── MarkdownEditor.kt
    └── adapter/
```

---

## 五、后端架构设计（PHP）

### 5.1 设计思路

PHP做后端的核心优势：**零编译、改了就生效、虚拟主机/免费主机都能跑、MySQL原生支持极好**。对个人项目来说，不用管进程管理、不用编译部署，写完FTP传上去就能用。

### 5.2 服务结构（原生PHP方案）

```
/var/www/selfdiscipline/
├── public/
│   └── index.php            # 唯一入口（前端所有请求打到这里）
├── src/
│   ├── Core/
│   │   ├── Router.php       # 轻量路由
│   │   ├── Request.php      # 请求封装
│   │   ├── Response.php     # JSON响应封装
│   │   ├── Database.php     # PDO单例连接
│   │   ├── Auth.php         # DeviceID验证
│   │   └── Config.php       # 配置文件（读环境变量/ini）
│   ├── Controllers/
│   │   ├── TaskController.php
│   │   ├── NoteController.php
│   │   ├── FitnessController.php
│   │   ├── MediaController.php
│   │   ├── HealthController.php
│   │   ├── LedgerController.php
│   │   ├── SyncController.php
│   │   └── ProxyController.php   # 第三方API代理
│   ├── Models/
│   │   ├── TaskModel.php
│   │   ├── NoteModel.php
│   │   └── ...
│   ├── Services/
│   │   ├── SyncService.php
│   │   ├── WeatherProxy.php
│   │   ├── TmdbProxy.php
│   │   └── EnglishProxy.php
│   └── Utils/
│       ├── Logger.php
│       └── Validator.php
├── config/
│   ├── database.php         # DB连接配置
│   ├── api_keys.php         # 第三方API Key（不入库，不进Git）
│   └── cors.php             # 跨域配置
├── sql/
│   └── schema.sql           # 建表SQL（一键导入）
└── .htaccess                # Nginx/Apache重写规则
```

### 5.3 路由设计

不引入框架，自己写一个50行的路由：

```php
// src/Core/Router.php
class Router {
    private $routes = [];
    
    public function add($method, $path, $handler) {
        $this->routes[] = ['method' => $method, 'path' => $path, 'handler' => $handler];
    }
    
    public function dispatch() {
        $method = $_SERVER['REQUEST_METHOD'];
        $path = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);
        $path = str_replace('/api/v1', '', $path);
        
        foreach ($this->routes as $route) {
            if ($route['method'] === $method && $this->match($route['path'], $path, $params)) {
                call_user_func_array($route['handler'], $params);
                return;
            }
        }
        Response::json(['code' => 404, 'message' => 'Not Found'], 404);
    }
    
    private function match($pattern, $path, &$params) {
        // 简单路径参数匹配：/tasks/:id
        $pattern = preg_replace('/:([a-z]+)/', '(?P<$1>[^/]+)', $pattern);
        if (preg_match("#^$pattern$#", $path, $matches)) {
            $params = array_filter($matches, 'is_string', ARRAY_FILTER_USE_KEY);
            return true;
        }
        return false;
    }
}
```

### 5.4 入口文件

```php
<?php
// public/index.php
require_once __DIR__ . '/../src/Core/Router.php';
require_once __DIR__ . '/../src/Core/Response.php';
require_once __DIR__ . '/../src/Core/Request.php';
require_once __DIR__ . '/../src/Core/Database.php';
require_once __DIR__ . '/../src/Core/Auth.php';
require_once __DIR__ . '/../config/database.php';

// 加载所有Controller
foreach (glob(__DIR__ . '/../src/Controllers/*.php') as $file) {
    require_once $file;
}

$router = new Router();

// 注册路由
$router->add('GET',    '/tasks',           [new TaskController(), 'list']);
$router->add('POST',   '/tasks',           [new TaskController(), 'batchUpsert']);
$router->add('DELETE', '/tasks/:id',       [new TaskController(), 'delete']);
$router->add('GET',    '/sync/pull',       [new SyncController(), 'pull']);
$router->add('POST',   '/sync/upload',     [new SyncController(), 'upload']);
$router->add('GET',    '/proxy/weather',   [new ProxyController(), 'weather']);
$router->add('GET',    '/proxy/tmdb/search', [new ProxyController(), 'tmdbSearch']);
// ...更多路由

$router->dispatch();
```

### 5.5 数据库操作示例（PDO）

```php
// src/Core/Database.php
class Database {
    private static $instance = null;
    private $conn;
    
    private function __construct() {
        $config = require __DIR__ . '/../../config/database.php';
        $dsn = "mysql:host={$config['host']};dbname={$config['dbname']};charset=utf8mb4";
        $this->conn = new PDO($dsn, $config['user'], $config['pass'], [
            PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
            PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
            PDO::ATTR_EMULATE_PREPARES => false,  // 防SQL注入
        ]);
    }
    
    public static function get() {
        if (self::$instance === null) {
            self::$instance = new self();
        }
        return self::$instance->conn;
    }
}
```

### 5.6 Controller示例

```php
// src/Controllers/TaskController.php
class TaskController {
    private $model;
    private $deviceId;
    
    public function __construct() {
        $this->model = new TaskModel();
        $this->deviceId = Auth::getDeviceId();  // 从Header读取并验证
    }
    
    public function list() {
        $since = $_GET['since'] ?? 0;
        $categoryId = $_GET['categoryId'] ?? null;
        
        $tasks = $this->model->getByDevice($this->deviceId, $since, $categoryId);
        Response::json(['code' => 0, 'data' => ['records' => $tasks]]);
    }
    
    public function batchUpsert() {
        $input = json_decode(file_get_contents('php://input'), true);
        $records = $input['records'] ?? [];
        
        $accepted = [];
        $rejected = [];
        
        foreach ($records as $record) {
            // 验证必填字段
            if (empty($record['id']) || empty($record['title'])) {
                $rejected[] = $record['id'] ?? 'unknown';
                continue;
            }
            // upsert（INSERT ... ON DUPLICATE KEY UPDATE）
            $this->model->upsert($this->deviceId, $record);
            $accepted[] = $record['id'];
        }
        
        Response::json(['code' => 0, 'data' => [
            'accepted' => $accepted,
            'rejected' => $rejected
        ]]);
    }
    
    public function delete($id) {
        $this->model->softDelete($this->deviceId, $id);
        Response::json(['code' => 0]);
    }
}
```

### 5.7 安全要点

| 措施 | 做法 |
|------|------|
| SQL注入 | 全部用PDO预处理，绝不拼接SQL |
| API Key保护 | `config/api_keys.php`存Key，加入`.gitignore` |
| DeviceID验证 | 每个请求必须带`X-Device-ID`，缺失返回401 |
| HTTPS | Nginx配置Let's Encrypt免费证书 |
| 输入验证 | 所有输入用`filter_var` + 白名单校验 |
| 错误隐藏 | 生产环境`display_errors = Off`，写日志不暴露 |

### 5.8 Nginx配置

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate     /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    root /var/www/selfdiscipline/public;
    index index.php;
    
    # 所有/api请求转发到index.php
    location /api/ {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    # PHP-FPM
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
    
    # 静态文件缓存
    location ~* \.(jpg|png|css|js)$ {
        expires 30d;
        add_header Cache-Control "public";
    }
}
```

---

## 六、数据库设计

### 6.1 同步字段规范

**所有表必须包含以下四个字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VARCHAR(36) | UUID，客户端生成 |
| `last_modified` | BIGINT | 毫秒时间戳 |
| `is_deleted` | TINYINT(1) | 软删除标记 |
| `device_id` | VARCHAR(36) | 设备ID（用户隔离） |

### 6.2 MySQL建表SQL

```sql
-- sql/schema.sql

CREATE DATABASE IF NOT EXISTS selfdiscipline 
  DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE selfdiscipline;

-- 任务表
CREATE TABLE IF NOT EXISTS tasks (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    title VARCHAR(500) NOT NULL,
    description TEXT NULL,
    category_id VARCHAR(36) NULL,
    task_type TINYINT NOT NULL DEFAULT 0 COMMENT '0=一次性 1=循环 2=长期目标',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0=待做 1=完成',
    priority TINYINT DEFAULT 3,
    due_date BIGINT NULL,
    repeat_type TINYINT DEFAULT 0 COMMENT '0=无 1=每日 2=每周',
    repeat_days VARCHAR(20) NULL,
    parent_goal_id VARCHAR(36) NULL,
    reminder_time BIGINT NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_modified (device_id, last_modified),
    INDEX idx_category (category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 分类表
CREATE TABLE IF NOT EXISTS categories (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    name VARCHAR(100) NOT NULL,
    icon_res VARCHAR(50) DEFAULT 'default',
    color_hex VARCHAR(10) DEFAULT '#4A90D9',
    sort_order INT DEFAULT 0,
    is_visible TINYINT(1) DEFAULT 1,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device (device_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 笔记表
CREATE TABLE IF NOT EXISTS notes (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    title VARCHAR(500) NOT NULL,
    content LONGTEXT NOT NULL,
    tags VARCHAR(500) NULL COMMENT '逗号分隔',
    image_paths JSON NULL,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_modified (device_id, last_modified),
    FULLTEXT INDEX ft_notes (title, content) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 运动记录表
CREATE TABLE IF NOT EXISTS fitness_records (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    exercise_name VARCHAR(200) NOT NULL,
    duration_minutes INT NOT NULL,
    record_date BIGINT NOT NULL,
    notes TEXT NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_date (device_id, record_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 影音书籍表
CREATE TABLE IF NOT EXISTS media_items (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    title VARCHAR(500) NOT NULL,
    type TINYINT NOT NULL COMMENT '0=书籍 1=电影',
    tmdb_id INT NULL,
    poster_url VARCHAR(1000) NULL,
    year INT NULL,
    description TEXT NULL,
    status TINYINT DEFAULT 0 COMMENT '0=想看 1=在看 2=已完成',
    rating TINYINT NULL COMMENT '1-5星',
    note_id VARCHAR(36) NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_type (device_id, type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 健康记录表
CREATE TABLE IF NOT EXISTS health_records (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    record_type TINYINT NOT NULL COMMENT '0=就诊 1=用药',
    hospital VARCHAR(200) NULL,
    department VARCHAR(100) NULL,
    diagnosis TEXT NULL,
    medication_name VARCHAR(200) NULL,
    dosage VARCHAR(100) NULL,
    frequency VARCHAR(100) NULL,
    start_date BIGINT NOT NULL,
    end_date BIGINT NULL,
    reminder_time BIGINT NULL,
    notes TEXT NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_type (device_id, record_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 记账表
CREATE TABLE IF NOT EXISTS ledger_entries (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    entry_type TINYINT NOT NULL COMMENT '0=支出 1=收入',
    amount DECIMAL(12,2) NOT NULL,
    category VARCHAR(50) NOT NULL,
    record_date BIGINT NOT NULL,
    note VARCHAR(500) NULL,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    INDEX idx_device_date (device_id, record_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 英语单词表
CREATE TABLE IF NOT EXISTS english_words (
    id VARCHAR(36) PRIMARY KEY,
    device_id VARCHAR(36) NOT NULL,
    word VARCHAR(200) NOT NULL,
    translation TEXT NULL,
    pronunciation VARCHAR(200) NULL,
    audio_url VARCHAR(1000) NULL,
    context TEXT NULL,
    mastery_level TINYINT DEFAULT 0,
    last_modified BIGINT NOT NULL,
    is_deleted TINYINT(1) DEFAULT 0,
    UNIQUE KEY uk_device_word (device_id, word),
    INDEX idx_device (device_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 同步元信息表
CREATE TABLE IF NOT EXISTS sync_meta (
    device_id VARCHAR(36) NOT NULL,
    table_name VARCHAR(50) NOT NULL,
    last_sync_time BIGINT DEFAULT 0,
    PRIMARY KEY (device_id, table_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 6.3 客户端Room实体（关键表）

```kotlin
// 抽象基类
abstract class BaseEntity {
    abstract val id: String
    abstract val lastModified: Long
    abstract val isDeleted: Boolean
    abstract val needsSync: Boolean
}

@Entity(tableName = "tasks")
data class TaskEntity(
    @PrimaryKey override val id: String = UUID.randomUUID().toString(),
    val title: String,
    val description: String? = null,
    val categoryId: String? = null,
    val taskType: Int = 0, // 0=一次性 1=循环 2=长期目标
    val status: Int = 0,
    val priority: Int = 3,
    val dueDate: Long? = null,
    val repeatType: Int = 0,
    val repeatDays: String? = null,
    val parentGoalId: String? = null,
    val reminderTime: Long? = null,
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

> 其余Entity与V1.0方案一致，字段与MySQL表一一对应。

---

## 七、接口文档草案

### 7.1 通用规范

**Base URL**：`https://your-domain.com/api/v1`

**请求头**：

| Header | 值 | 说明 |
|--------|-----|------|
| X-Device-ID | UUID | 设备唯一标识 |
| Content-Type | application/json | 请求体格式 |
| Accept | application/json | 响应格式 |

**通用响应格式**：

```json
{
  "code": 0,
  "message": "success",
  "data": { },
  "serverTime": 1719600000000
}
```

**错误码**：

| Code | 说明 |
|------|------|
| 0 | 成功 |
| 400 | 请求参数错误 |
| 401 | 设备ID缺失或无效 |
| 404 | 资源不存在 |
| 409 | 版本冲突 |
| 500 | 服务器内部错误 |

### 7.2 同步接口（核心）

#### 7.2.1 批量上传

```
POST /sync/upload
Content-Type: application/json
X-Device-ID: uuid-xxx

{
  "tableName": "tasks",
  "records": [
    {
      "id": "uuid-xxx",
      "title": "晨跑",
      "taskType": 1,
      "lastModified": 1719600000000,
      "isDeleted": false,
      "...": "...其他业务字段"
    }
  ]
}
```

**PHP端处理**：

```php
// SyncController.php - upload
public function upload() {
    $input = json_decode(file_get_contents('php://input'), true);
    $tableName = $input['tableName'];
    $records = $input['records'] ?? [];
    
    // 白名单校验表名，防止SQL注入
    $allowedTables = ['tasks', 'categories', 'notes', 'fitness_records', 
                      'media_items', 'health_records', 'ledger_entries', 'english_words'];
    if (!in_array($tableName, $allowedTables)) {
        Response::json(['code' => 400, 'message' => 'Invalid table'], 400);
        return;
    }
    
    $db = Database::get();
    $db->beginTransaction();
    
    $accepted = [];
    $rejected = [];
    
    foreach ($records as $record) {
        try {
            // INSERT ... ON DUPLICATE KEY UPDATE
            $fields = array_keys($record);
            $placeholders = array_fill(0, count($fields), '?');
            
            $updateFields = array_diff($fields, ['id']);
            $updateClause = implode(', ', array_map(fn($f) => "$f = VALUES($f)", $updateFields));
            
            $sql = "INSERT INTO {$tableName} (" . implode(', ', $fields) . ") 
                    VALUES (" . implode(', ', $placeholders) . ")
                    ON DUPLICATE KEY UPDATE {$updateClause}";
            
            $stmt = $db->prepare($sql);
            $stmt->execute(array_values($record));
            $accepted[] = $record['id'];
        } catch (Exception $e) {
            $rejected[] = $record['id'] ?? 'unknown';
        }
    }
    
    $db->commit();
    
    // 更新sync_meta
    $this->updateSyncTime($tableName);
    
    Response::json(['code' => 0, 'data' => [
        'accepted' => $accepted,
        'rejected' => $rejected
    ]]);
}
```

#### 7.2.2 增量拉取

```
GET /sync/pull?tableName=tasks&since=1719500000000
```

**PHP端处理**：

```php
public function pull() {
    $tableName = $_GET['tableName'] ?? '';
    $since = intval($_GET['since'] ?? 0);
    
    $allowedTables = ['tasks', 'categories', 'notes', 'fitness_records', 
                      'media_items', 'health_records', 'ledger_entries', 'english_words'];
    if (!in_array($tableName, $allowedTables)) {
        Response::json(['code' => 400, 'message' => 'Invalid table'], 400);
        return;
    }
    
    $db = Database::get();
    $stmt = $db->prepare("SELECT * FROM {$tableName} 
                         WHERE device_id = ? AND last_modified > ? AND is_deleted = 0");
    $stmt->execute([$this->deviceId, $since]);
    $records = $stmt->fetchAll();
    
    Response::json(['code' => 0, 'data' => [
        'records' => $records,
        'serverTime' => time() * 1000
    ]]);
}
```

#### 7.2.3 全量拉取（首次安装）

```
GET /sync/full?tableName=tasks
```

### 7.3 各模块接口

| 方法 | URL | 说明 |
|------|-----|------|
| GET | /tasks?categoryId=&since= | 任务列表 |
| POST | /tasks | 批量创建/更新 |
| DELETE | /tasks/:id | 删除任务 |
| GET | /tasks/stats?month=2026-07 | 月度统计 |
| GET | /categories | 分类列表 |
| POST | /categories | 创建/更新分类 |
| DELETE | /categories/:id | 删除分类 |
| GET | /notes | 笔记列表 |
| GET | /notes/:id | 笔记详情 |
| POST | /notes | 创建/更新笔记 |
| DELETE | /notes/:id | 删除笔记 |
| GET | /fitness?month=2026-07 | 运动记录 |
| POST | /fitness | 新增/更新 |
| DELETE | /fitness/:id | 删除 |
| GET | /media?type=0/1 | 影音列表 |
| POST | /media | 创建/更新 |
| DELETE | /media/:id | 删除 |
| GET | /health | 健康记录 |
| POST | /health | 创建/更新 |
| DELETE | /health/:id | 删除 |
| GET | /ledger?month=2026-07 | 记账列表 |
| POST | /ledger | 创建/更新 |
| DELETE | /ledger/:id | 删除 |
| GET | /ledger/stats?month=2026-07 | 月度统计 |
| GET | /english/words | 单词本 |
| POST | /english/words | 添加单词 |

### 7.4 第三方API代理接口

| 方法 | URL | 说明 |
|------|-----|------|
| GET | /proxy/weather?city=邢台 | 天气查询（服务端调有道/和风） |
| GET | /proxy/tmdb/search?query=xxx | TMDB电影搜索 |
| GET | /proxy/translate?text=hello&to=en | 翻译 |
| GET | /proxy/word?text=hello | 单词查询 |

> **关键**：所有第三方API Key存在`config/api_keys.php`中，PHP服务端调用，**绝不下发到客户端**。

---

## 八、离线同步方案

### 8.1 同步策略

**核心原则**：客户端本地DB是唯一可信源 → 乐观更新 → 后台异步同步

### 8.2 字段约定

每个Entity包含四个同步字段：

```kotlin
abstract class SyncableEntity {
    abstract val id: String              // UUID，客户端生成
    abstract val lastModified: Long      // 最后修改时间(ms)
    abstract val isDeleted: Boolean      // 软删除
    abstract val needsSync: Boolean      // 待同步标记
}
```

### 8.3 同步流程

```
用户操作
   │
   ▼
写入本地DB + needsSync = true
   │
   ▼
UI立即更新（乐观更新）
   │
   ▼
SyncManager监听网络变化
   │
   ├── 有网 ──→ SyncWorker启动
   │              │
   │              ▼
   │        1. 收集所有needsSync=true的记录
   │        2. POST到 /sync/upload
   │        3. 收到成功响应 → needsSync=false
   │        4. 拉取增量 /sync/pull?since=lastSyncTime
   │        5. 更新本地DB
   │
   └── 无网 ──→ 操作入队，等待网络恢复
```

### 8.4 WorkManager配置

```kotlin
class SyncWorker(
    context: Context,
    params: WorkerParameters
) : CoroutineWorker(context, params) {

    override suspend fun doWork(): Result {
        return try {
            syncManager.uploadPendingChanges()
            syncManager.pullIncrementalChanges()
            Result.success()
        } catch (e: Exception) {
            Result.retry()
        }
    }

    companion object {
        fun schedule(context: Context) {
            val constraints = Constraints.Builder()
                .setRequiredNetworkType(NetworkType.CONNECTED)
                .build()

            val request = PeriodicWorkRequestBuilder<SyncWorker>(
                repeatInterval = 15, TimeUnit.MINUTES
            )
                .setConstraints(constraints)
                .setBackoffCriteria(
                    BackoffPolicy.EXPONENTIAL,
                    30, TimeUnit.SECONDS
                )
                .build()

            WorkManager.getInstance(context)
                .enqueueUniquePeriodicWork(
                    "sync", ExistingPeriodicWorkPolicy.KEEP, request
                )
        }
    }
}
```

### 8.5 冲突解决策略

**采用 Last-Write-Wins (LWW)**：

```kotlin
class ConflictResolver {
    fun resolve(local: Entity, remote: Entity): Entity {
        return if (local.lastModified > remote.lastModified) {
            local.copy(needsSync = true)  // 本地更新，需要上传
        } else {
            remote.copy(needsSync = false) // 接受服务端版本
        }
    }
}
```

**PHP端Upsert逻辑**：

```php
// 使用 MySQL 的 INSERT ... ON DUPLICATE KEY UPDATE
// 冲突时比较 last_modified，大的覆盖小的

$sql = "INSERT INTO tasks (id, device_id, title, last_modified, ...) 
        VALUES (?, ?, ?, ?, ...)
        ON DUPLICATE KEY UPDATE 
        title = IF(VALUES(last_modified) > last_modified, VALUES(title), title),
        last_modified = GREATEST(last_modified, VALUES(last_modified)),
        ...";
```

**删除保护**：已删除记录保留30天，防止误删后同步恢复。

### 8.6 首次启动流程

```
App启动
  │
  ▼
读取本地DeviceID（没有则生成UUID）
  │
  ▼
检查本地是否有数据
  │
  ├── 有数据 ──→ 正常启动 + 后台触发同步
  │
  └── 无数据 ──→ 调用 /sync/full 逐表全量拉取
                 标记所有数据为已同步
```

---

## 九、通知提醒方案

### 9.1 架构

```
NotificationScheduler (统一调度入口)
        │
        ├── AlarmManager (精确闹钟，适合复诊提醒)
        │
        └── WorkManager (延迟任务，适合任务提醒)
                │
                ▼
        NotificationReceiver (接收广播)
                │
                ▼
        NotificationManager (展示通知)
```

### 9.2 权限要求

| 权限 | API级别 | 说明 |
|------|---------|------|
| POST_NOTIFICATIONS | Android 13+ (33) | 运行时申请 |
| SCHEDULE_EXACT_ALARM | Android 12+ (31) | 精确闹钟，需引导开启 |
| RECEIVE_BOOT_COMPLETED | 所有版本 | 开机恢复闹钟 |

### 9.3 核心代码

```kotlin
class NotificationScheduler @Inject constructor(
    private val context: Context
) {
    private val alarmManager = context.getSystemService(AlarmManager::class.java)
    private val notificationManager = context.getSystemService(NotificationManager::class.java)

    fun scheduleTaskReminder(taskId: String, triggerAt: Long, title: String) {
        val intent = Intent(context, NotificationReceiver::class.java).apply {
            putExtra(EXTRA_TASK_ID, taskId)
            putExtra(EXTRA_TITLE, title)
            putExtra(EXTRA_TYPE, TYPE_TASK)
        }
        val pi = PendingIntent.getBroadcast(
            context, taskId.hashCode(), intent,
            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
        )
        alarmManager.setExactAndAllowWhileIdle(
            AlarmManager.RTC_WAKEUP, triggerAt, pi
        )
    }

    fun scheduleMedicationReminder(recordId: String, times: List<Long>) {
        times.forEachIndexed { index, time ->
            val intent = Intent(context, NotificationReceiver::class.java).apply {
                putExtra(EXTRA_RECORD_ID, recordId)
                putExtra(EXTRA_TYPE, TYPE_MEDICATION)
            }
            val pi = PendingIntent.getBroadcast(
                context, "${recordId}_$index".hashCode(), intent,
                PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
            )
            alarmManager.setRepeating(
                AlarmManager.RTC_WAKEUP, time,
                AlarmManager.INTERVAL_DAY, pi
            )
        }
    }

    fun cancelReminder(id: String) {
        val intent = Intent(context, NotificationReceiver::class.java)
        val pi = PendingIntent.getBroadcast(
            context, id.hashCode(), intent,
            PendingIntent.FLAG_NO_CREATE or PendingIntent.FLAG_IMMUTABLE
        )
        pi?.let { alarmManager.cancel(it) }
    }
}
```

### 9.4 开机恢复

```kotlin
class BootReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        if (intent.action == Intent.ACTION_BOOT_COMPLETED) {
            val scheduler = NotificationScheduler(context)
            val repository = ... 
            CoroutineScope(Dispatchers.IO).launch {
                repository.getAllActiveReminders().forEach { reminder ->
                    scheduler.scheduleTaskReminder(
                        reminder.id, reminder.triggerTime, reminder.title
                    )
                }
            }
        }
    }
}
```

### 9.5 通知渠道

| 渠道ID | 名称 | 重要性 | 用途 |
|--------|------|--------|------|
| task_reminder | 任务提醒 | HIGH | 待办到期 |
| medication_reminder | 用药提醒 | HIGH | 服药闹钟 |
| revisit_reminder | 复诊提醒 | MAX | 复诊闹钟 |
| sync_status | 同步通知 | LOW | 同步失败提示 |

---

## 十、第三方API接入

### 10.1 天气API（和风天气）

**注册**：https://dev.qweather.com → 免费版够用

**PHP代理实现**：

```php
// src/Services/WeatherProxy.php
class WeatherProxy {
    public function getWeather($city) {
        $config = require __DIR__ . '/../../config/api_keys.php';
        $apiKey = $config['qweather_key'];
        
        $url = "https://devapi.qweather.com/v7/weather/now?location={$city}&key={$apiKey}";
        
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        curl_setopt($ch, CURLOPT_TIMEOUT, 10);
        $response = curl_exec($ch);
        curl_close($ch);
        
        return json_decode($response, true);
    }
}
```

**Android端调用**：

```kotlin
interface WeatherApi {
    @GET("proxy/weather")
    suspend fun getWeather(@Query("city") city: String): WeatherResponse
}
```

### 10.2 TMDB电影API

**注册**：https://www.themoviedb.org/settings/api → 免费

**PHP代理实现**：

```php
// src/Services/TmdbProxy.php
class TmdbProxy {
    public function search($query, $page = 1) {
        $config = require __DIR__ . '/../../config/api_keys.php';
        $apiKey = $config['tmdb_key'];
        
        $url = "https://api.themoviedb.org/3/search/movie?" . http_build_query([
            'api_key' => $apiKey,
            'query' => $query,
            'language' => 'zh-CN',
            'page' => $page
        ]);
        
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        curl_setopt($ch, CURLOPT_TIMEOUT, 10);
        $response = curl_exec($ch);
        curl_close($ch);
        
        return json_decode($response, true);
    }
    
    public function getPosterUrl($posterPath, $size = 'w500') {
        return "https://image.tmdb.org/t/p/{$size}{$posterPath}";
    }
}
```

**Android端搜索流程**：

```
用户输入关键词
    │
    ▼
调用 /proxy/tmdb/search?query=xxx
    │
    ▼
PHP服务端用TMDB Key请求API
    │
    ▼
返回结果列表 → 展示海报+标题+年份
    │
    ▼
用户选择 → 自动填充title/posterUrl/year/description
    │
    ▼
用户补充 status / rating / noteId → 保存
```

### 10.3 英语API（有道智云）

**注册**：https://ai.youdao.com → 创建应用获取Key

**PHP代理实现**：

```php
// src/Services/EnglishProxy.php
class EnglishProxy {
    private $appKey;
    private $appSecret;
    
    public function __construct() {
        $config = require __DIR__ . '/../../config/api_keys.php';
        $this->appKey = $config['youdao_app_key'];
        $this->appSecret = $config['youdao_app_secret'];
    }
    
    public function translate($text, $from = 'auto', $to = 'en') {
        $salt = rand(10000, 99999);
        $sign = md5($this->appKey . $text . $salt . $this->appSecret);
        
        $params = [
            'q' => $text,
            'from' => $from,
            'to' => $to,
            'appKey' => $this->appKey,
            'salt' => $salt,
            'sign' => $sign
        ];
        
        $url = "https://openapi.youdao.com/api?" . http_build_query($params);
        
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        $response = curl_exec($ch);
        curl_close($ch);
        
        return json_decode($response, true);
    }
    
    public function lookupWord($word) {
        // 单词查询接口（有道词典API）
        $salt = rand(10000, 99999);
        $sign = md5($this->appKey . $word . $salt . $this->appSecret);
        
        $params = [
            'q' => $word,
            'appKey' => $this->appKey,
            'salt' => $salt,
            'sign' => $sign
        ];
        
        $url = "https://openapi.youdao.com/api?" . http_build_query($params);
        
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        $response = curl_exec($ch);
        curl_close($ch);
        
        return json_decode($response, true);
    }
}
```

### 10.4 API Key安全管理

**原则**：所有第三方API Key只存在后端PHP配置文件，前端通过后端代理调用。

```
Android App
    │
    ▼
PHP代理接口 (API Key在config/api_keys.php中)
    │
    ▼
第三方API (TMDB / 有道 / 和风)
```

**config/api_keys.php**（不进Git，通过`.gitignore`排除）：

```php
<?php
return [
    'qweather_key'    => 'your-qweather-api-key',
    'tmdb_key'       => 'your-tmdb-api-key',
    'youdao_app_key' => 'your-youdao-app-key',
    'youdao_app_secret' => 'your-youdao-app-secret',
];
```

**`.gitignore`**：

```
config/api_keys.php
*.log
vendor/
.DS_Store
```

---

## 十一、页面清单与导航结构

### 11.1 页面总览

| 页面 | 类型 | 说明 |
|------|------|------|
| 首页 | Activity | 天气卡片+各模块概览+快捷入口 |
| 任务列表 | Activity | 按分类展示任务 |
| 任务编辑 | Activity | 新建/编辑任务 |
| 任务统计 | Activity | 日历+完成率+图表 |
| 分类管理 | Activity | 增删改分类、排序 |
| 运动记录 | Activity | 列表+新增 |
| 英语首页 | Activity | 翻译/单词本/跟读入口 |
| 翻译页 | Activity | 输入文字→翻译结果 |
| 单词本 | Activity | 单词列表+详情 |
| 跟读练习 | Activity | 录音+播放对比 |
| 影音书籍列表 | Activity | Tab切换(书籍/电影) |
| 电影搜索 | Activity | TMDB搜索选片 |
| 影音详情 | Activity | 状态/评分/关联笔记 |
| 健康记录 | Activity | 就诊+用药列表 |
| 健康编辑 | Activity | 新增/编辑记录 |
| 记账 | Activity | 列表+统计 |
| 记账编辑 | Activity | 新增/编辑收支 |
| 笔记列表 | Activity | 笔记卡片列表 |
| 笔记编辑 | Activity | Markdown编辑器 |
| 笔记预览 | Activity | Markdown渲染 |
| 全局搜索 | Activity | 跨模块搜索结果 |
| 设置 | Activity | 主题/板块/关于 |
| 主题设置 | Activity | 浅色/深色/跟随系统 |
| 板块开关 | Activity | 勾选首页显示哪些模块 |

### 11.2 导航结构

```
                    ┌──────────┐
                    │   首页    │
                    └────┬─────┘
         ┌──────┬────┬───┴───┬──────┬───────┐
         ▼      ▼    ▼       ▼      ▼       ▼
    ┌────────┐ ┌──┐ ┌──┐ ┌────────┐ ┌──┐ ┌──────┐
    │任务打卡│ │运动│ │英语│ │影音书籍│ │健康│ │记账  │
    └───┬────┘ └──┘ └──┘ └───┬────┘ └──┘ └──┬───┘
        │                    │              │
        ▼                    ▼              ▼
   统计/分类管理        搜索TMDB        编辑/统计
   编辑/详情            详情/状态
   
   ┌────────┐
   │ 笔记   │──编辑──预览
   └───┬────┘
       │
       ▼
   全局搜索（底部导航或首页搜索框进入）
   
   ┌────────┐
   │ 设置   │──主题──板块开关
   └────────┘
```

### 11.3 首页布局

```
┌─────────────────────────┐
│  [天气卡片] 28°C 晴     │
├─────────────────────────┤
│  📋 今日任务 (3/5)      │
│  ├ 晨跑 ✅               │
│  ├ 背单词 ⬜              │
│  └ 看论文 ⬜              │
│  [查看全部 →]            │
├─────────────────────────┤
│  💪 运动 (本月12h)      │
│  [记一笔 →]             │
├─────────────────────────┤
│  📖 阅读中 (2本)        │
│  [管理 →]               │
├─────────────────────────┤
│  🎬 想看 (5部)          │
│  [搜索添加 →]           │
├─────────────────────────┤
│  💊 用药提醒 (2个)     │
│  [管理 →]              │
├─────────────────────────┤
│  💰 本月支出 ¥3280     │
│  [记账 →]              │
├─────────────────────────┤
│  ✏️ 最近笔记            │
│  [写笔记 →]            │
└─────────────────────────┘
```

> 以上模块均可通过设置页开关显示/隐藏。

---

## 十二、主题与UI规范

### 12.1 主题架构

```kotlin
// ThemeManager.kt
class ThemeManager @Inject constructor(
    private val dataStore: DataStore<Preferences>
) {
    enum class ThemeMode { LIGHT, DARK, SYSTEM }
    
    val themeFlow: Flow<ThemeMode> = dataStore.data.map {
        ThemeMode.valueOf(it[THEME_KEY] ?: ThemeMode.SYSTEM.name)
    }
    
    suspend fun setTheme(mode: ThemeMode) {
        dataStore.edit { it[THEME_KEY] = mode.name }
    }
}
```

### 12.2 深色模式实现

```kotlin
fun applyTheme(mode: ThemeMode) {
    when (mode) {
        ThemeMode.LIGHT -> AppCompatDelegate.setDefaultNightMode(MODE_NIGHT_NO)
        ThemeMode.DARK -> AppCompatDelegate.setDefaultNightMode(MODE_NIGHT_YES)
        ThemeMode.SYSTEM -> AppCompatDelegate.setDefaultNightMode(MODE_NIGHT_FOLLOW_SYSTEM)
    }
}
```

### 12.3 配色方案

**浅色模式**：

| 用途 | 色值 | 说明 |
|------|------|------|
| Primary | #4A90D9 | 主色调(蓝) |
| Primary Dark | #2E6FBF | 深色变体 |
| Accent | #FF6B6B | 强调色(红) |
| Background | #F5F5F5 | 页面背景 |
| Surface | #FFFFFF | 卡片背景 |
| Text Primary | #212121 | 主文字 |
| Text Secondary | #757575 | 次文字 |
| Divider | #E0E0E0 | 分割线 |

**深色模式**：

| 用途 | 色值 | 说明 |
|------|------|------|
| Primary | #5BA3F5 | 主色调(亮蓝) |
| Background | #121212 | 页面背景 |
| Surface | #1E1E1E | 卡片背景 |
| Text Primary | #E0E0E0 | 主文字 |
| Text Secondary | #AAAAAA | 次文字 |
| Divider | #333333 | 分割线 |

### 12.4 板块开关实现

```kotlin
data class HomeModuleConfig(
    val showWeather: Boolean = true,
    val showTasks: Boolean = true,
    val showFitness: Boolean = true,
    val showEnglish: Boolean = true,
    val showMedia: Boolean = true,
    val showHealth: Boolean = true,
    val showLedger: Boolean = true,
    val showNotes: Boolean = true
)

class HomeAdapter(
    private val config: HomeModuleConfig,
    private val data: HomeData
) : RecyclerView.Adapter<RecyclerView.ViewHolder>() {
    
    private val visibleModules: List<ModuleType>
        get() {
            val list = mutableListOf<ModuleType>()
            if (config.showWeather) list.add(ModuleType.WEATHER)
            if (config.showTasks) list.add(ModuleType.TASKS)
            if (config.showFitness) list.add(ModuleType.FITNESS)
            if (config.showMedia) list.add(ModuleType.MEDIA)
            if (config.showHealth) list.add(ModuleType.HEALTH)
            if (config.showLedger) list.add(ModuleType.LEDGER)
            if (config.showNotes) list.add(ModuleType.NOTES)
            return list
        }
}
```

---

## 十三、开发阶段计划

### 阶段0：方案锁定（1-2天）

| 任务 | 产出 |
|------|------|
| 确认数据库表结构 | 本文档第六章SQL |
| 确认接口草案 | 本文档第七章 |
| 申请第三方API Key | TMDB/和风/有道账号 |
| **服务器环境搭建** | **免费云主机 + Nginx + PHP 8.2 + MySQL** |
| **域名解析配置** | **DNS解析到服务器IP + HTTPS证书** |
| Git仓库初始化 | GitHub/Gitee Private Repo |

### 阶段1：项目地基（3-5天）

| 任务 | 说明 |
|------|------|
| Android项目初始化 | Kotlin + Gradle KTS + Hilt |
| Room数据库搭建 | 所有Entity + DAO + Database |
| Retrofit封装 | OkHttp + 拦截器 + 错误处理 |
| DataStore配置 | 主题偏好 + 板块开关 + DeviceID |
| 深浅色主题 | Material3 两套配色 |
| 主页框架 | 底部导航 + 板块卡片 + 开关控制 |
| **PHP后端初始化** | **目录结构 + 路由 + DB连接 + 第一个测试接口** |

**✅ 里程碑**：App能启动、切主题、本地DB能读写、PHP接口能返回数据

### 阶段2：核心任务模块（5-7天）

| 任务 | 说明 |
|------|------|
| 分类管理 | 增删改查 + 排序 + 颜色图标 |
| 三种任务CRUD | 一次性/循环/长期目标 |
| 任务列表UI | 按分类筛选 + 排序 + 完成动画 |
| 循环任务计算 | 下次到期时间工具类 |
| 日历视图 | 标记有任务的日期 |
| 月度统计 | 完成率数字 + 进度条 |
| **PHP任务接口** | **list/batchUpsert/delete + 统计查询** |

**✅ 里程碑**：本地可用的完整打卡App + PHP端能存取数据

### 阶段3：通用能力（4-6天）

| 任务 | 说明 |
|------|------|
| Markdown笔记 | Markwon集成 + 编辑 + 预览 + 图片上传 |
| 全局搜索 | Room FTS4 + 跨表搜索UI |
| 本地通知 | AlarmManager封装 + 任务提醒 |
| 记账模块 | CRUD + 分类 + MPAndroidChart统计 |
| **PHP笔记/记账接口** | **对应表CRUD + 全文搜索SQL** |

**✅ 里程碑**：笔记可写、搜索可用、通知能弹、记账能统计

### 阶段4：专项模块一期（4-5天）

| 任务 | 说明 |
|------|------|
| 运动记录 | 项目+时长+历史列表+月度统计 |
| 影音书籍 | 书籍CRUD + 电影TMDB搜索 + 状态评分 |
| 健康记录 | 就诊+用药+复诊提醒(复用通知) |
| **PHP对应接口** | **三个模块CRUD** |

**✅ 里程碑**：四个专项模块全部可用

### 阶段5：云端同步（5-7天）

| 任务 | 说明 |
|------|------|
| **PHP同步接口** | **/sync/upload + /sync/pull + /sync/full** |
| **PHP Upsert逻辑** | **INSERT ON DUPLICATE KEY UPDATE + 冲突解决** |
| SyncManager | 上传/拉取/冲突解决 |
| SyncWorker | WorkManager周期+网络监听 |
| 同步状态UI | 同步中/失败/上次同步时间 |
| 全量+增量 | 首次全量 + 日常增量 |
| 离线测试 | 断网操作 → 联网自动同步 |

**✅ 里程碑**：数据上云、离线自动同步、换设备数据恢复

### 阶段6：第三方API（3-4天）

| 任务 | 说明 |
|------|------|
| **PHP代理接口** | **天气/TMDB/翻译代理（Key在服务端）** |
| 天气模块 | 首页卡片 + 定位 + 展示 |
| 英语模块 | 翻译页 + 单词本 + 录音跟读 |
| 图表优化 | 任务统计换MPAndroidChart正经图表 |

**✅ 里程碑**：天气/电影搜索/英语翻译全部可用

### 阶段7：打磨上架（3-5天）

| 任务 | 说明 |
|------|------|
| 回归测试 | 全模块 + 离线 + 同步 + 通知 |
| 空页面优化 | 占位图 + 引导文案 |
| 权限引导 | 通知/精确闹钟/定位 |
| 崩溃收集 | Crashlytics接入 |
| 性能优化 | 列表懒加载 + DB查询优化 |
| 打包签名 | Release APK/AAB |
| 上架素材 | 图标 + 截图 + 描述 |

**✅ 里程碑**：可上架的完整产品

### 总工期估算

| 阶段 | 工期 | 累计 |
|------|------|------|
| 阶段0 | 1-2天 | 2天 |
| 阶段1 | 3-5天 | 7天 |
| 阶段2 | 5-7天 | 14天 |
| 阶段3 | 4-6天 | 20天 |
| 阶段4 | 4-5天 | 25天 |
| 阶段5 | 5-7天 | 32天 |
| 阶段6 | 3-4天 | 36天 |
| 阶段7 | 3-5天 | 41天 |

> **总计约6-8周**（每天4-6小时有效开发时间）

---

## 十四、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 设备ID丢失导致数据孤岛 | 重装后无法找回数据 | 云端为主，定期提醒用户勿清数据；后期可加导出恢复码 |
| 同步冲突导致数据丢失 | 用户操作被覆盖 | LWW + 30天删除保护 + 同步日志可查 |
| 后台同步被系统杀死 | 离线数据迟迟不上传 | WorkManager + 前台服务双保险 + 打开App时立即触发 |
| API Key泄露 | 额度被盗刷 | 全部走PHP后端代理，前端不存任何Key |
| TMDB API限流 | 搜索失败 | 客户端缓存搜索结果 + 服务端限流 |
| 精确闹钟被限制 | 复诊提醒不响 | Android 12+引导用户手动开启权限 + 降级方案用WorkManager |
| 全文搜索性能差 | 搜索卡顿 | FTS/ngram索引 + 分页 + 数据量超1万条时优化 |
| 服务器宕机 | App不可用 | 本地缓存兜底，无网时正常浏览 |
| 个人开发时间不足 | 项目搁置 | 模块化开发，每个阶段独立可交付，随时可暂停 |
| PHP版本兼容性 | 代码在某些主机跑不了 | 锁定PHP 8.2+，使用严格类型声明 |
| 免费主机限制 | 部分免费主机不支持自定义PHP/Cron | 选对主机（见附录B），避免踩坑 |

---

## 十五、不做的事（范围边界）

> 写在这墙上，防止范围蔓延 🚧

- ❌ 登录注册（手机号/密码/第三方）
- ❌ 多用户/多设备切换
- ❌ 数据导出（Excel/JSON/备份文件）
- ❌ 社区/分享/社交功能
- ❌ 拖拽自由排版
- ❌ 自定义配色主题
- ❌ 复杂健身参数（重量/组数/体脂率）
- ❌ 剧集季/集管理（先只做电影，预留type字段）
- ❌ 图片云端同步（笔记图片存本地，不同步）
- ❌ 服务端推送（只用客户端本地通知）
- ❌ 广告/付费/内购

---

## 附录A：Git分支策略

```
main          ── 稳定可发布版本
develop       ── 日常开发
feature/x     ── 各功能模块分支
release/x.x   ── 发布前测试
hotfix/x      ── 紧急修复
```

每个阶段结束打Tag：`v0.1` `v0.2` ... `v1.0`

**仓库建议**：
- Android客户端：一个Repo
- PHP后端：一个Repo（或用子目录`server/`放在同一个Repo里）
- 推荐分开，方便部署时只拉后端代码到服务器

---

## 附录B：服务器选型清单

### B.1 免费/低价云主机推荐

| 服务商 | 套餐 | 配置 | 价格 | 适合度 |
|--------|------|------|------|--------|
| **Oracle Cloud** | Always Free | 2核1G ARM + 50G存储 | **永久免费** | ⭐⭐⭐⭐⭐ |
| **Google Cloud** | Free Tier | 1核0.6G + 30G | **永久免费** | ⭐⭐⭐⭐ |
| **AWS** | Free Tier | 1核1G (12个月) | 首年免费 | ⭐⭐⭐ |
| **腾讯云** | 轻量应用服务器 | 2核2G | ~¥40/月 | ⭐⭐⭐⭐ |
| **阿里云** | 轻量应用服务器 | 2核2G | ~¥50/月 | ⭐⭐⭐⭐ |
| **华为云** | 弹性云服务器 | 1核2G | ~¥35/月 | ⭐⭐⭐ |

### B.2 强烈推荐：Oracle Cloud 永久免费套餐

> 这是个人项目的最优解，不用花一分钱，配置还够用。

**包含资源**：
- 2台 ARM Ampere A1 实例（共4核24G，可拆成1台2核4G）
- 200G块存储（可拆成2块50G+100G）
- 10G对象存储
- 每月10TB出口流量
- 免费公网IP

**部署步骤**：

```bash
# 1. 注册Oracle Cloud（需要信用卡验证，不会扣费）
# 2. 创建Compute实例
#    - 镜像选 Ubuntu 22.04 LTS
#    - Shape选 ARM Ampere (2 cores, 4GB RAM)
#    - 公网IP自动分配
#    - SSH密钥提前生成好

# 3. SSH连上后一键安装环境
ssh ubuntu@your-public-ip

# 4. 安装Nginx + PHP + MySQL
sudo apt update
sudo apt install -y nginx mysql-server php8.2-fpm php8.2-mysql php8.2-curl

# 5. 配置MySQL
sudo mysql_secure_installation
sudo mysql -e "CREATE DATABASE selfdiscipline CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 6. 上传项目代码
scp -r ./server ubuntu@your-public-ip:/var/www/selfdiscipline

# 7. 配置Nginx（参考本文档第五章8节配置）
# 8. 申请HTTPS证书
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### B.3 最低配置要求

| 资源 | 最低 | 推荐 |
|------|------|------|
| CPU | 1核 | 2核 |
| 内存 | 512MB（PHP很省） | 2GB |
| 硬盘 | 20GB | 50GB |
| 带宽 | 1Mbps | 5Mbps |
| 系统 | Ubuntu 22.04 LTS | Ubuntu 22.04 LTS |
| PHP | 8.1+ | 8.2+ |
| MySQL | 5.7+ | 8.0+ |
| Nginx | 1.18+ | 1.24+ |

> PHP + MySQL对个人项目极其省资源，1核512MB跑这套系统绰绰有余，QPS 50以内完全没压力。

### B.4 域名配置

既然你自备域名，只需做两步：

1. **DNS解析**：在域名服务商控制台添加A记录，指向服务器公网IP
2. **HTTPS证书**：用Let's Encrypt免费证书（certbot一行命令搞定）

```bash
# 安装certbot
sudo apt install -y certbot python3-certbot-nginx

# 自动申请并配置证书
sudo certbot --nginx -d your-domain.com

# 自动续期（certbot安装时已自动配置cron）
sudo certbot renew --dry-run
```

---

## 附录C：关键决策记录

| 决策 | 选项 | 最终选择 | 理由 |
|------|------|----------|------|
| 后端语言 | Ktor/Node/Go/PHP | **PHP** | 零编译、虚拟主机随便跑、MySQL原生支持、生态成熟、个人项目改了就生效 |
| 后端框架 | 原生PHP/Laravel/Slim | **原生PHP**（可选Slim） | 项目规模小，框架反而增加复杂度 |
| 数据库 | MySQL/PostgreSQL/SQLite | **MySQL** | 资料最多、phpMyAdmin好用、免费主机默认支持 |
| 服务器 | 付费轻量云/免费云 | **免费优先**（Oracle Cloud） | 个人项目零成本，配置够用 |
| 域名 | 新购/自备 | **自备** | 已有域名，省一笔费用 |
| HTTPS | 付费证书/免费 | **Let's Encrypt** | 免费、自动续期、被所有客户端信任 |
| 同步策略 | 实时WebSocket/轮询 | 周期+事件触发 | 个人项目不需要实时 |
| 冲突解决 | LWW/CRDT/手动 | LWW | 实现简单，个人使用冲突极少 |
| 图片存储 | 本地/OSS/CDN | 本地+TMDB CDN | 零成本，个人够用 |
| 通知方案 | FCM/本地 | 本地AlarmManager | 无登录不需要FCM |
| 英语API | 有道/百度/Google | 有道智云 | 中文支持好，免费额度够 |
| API Key安全 | 前端硬编码/后端代理 | 后端PHP代理 | Key永远不下发客户端 |

---

## 附录D：PHP后端目录结构与示例

### D.1 完整目录树

```
selfdiscipline-server/
├── public/
│   └── index.php            # 唯一入口
├── src/
│   ├── Core/
│   │   ├── Router.php
│   │   ├── Request.php
│   │   ├── Response.php
│   │   ├── Database.php
│   │   ├── Auth.php
│   │   └── Config.php
│   ├── Controllers/
│   │   ├── TaskController.php
│   │   ├── CategoryController.php
│   │   ├── NoteController.php
│   │   ├── FitnessController.php
│   │   ├── MediaController.php
│   │   ├── HealthController.php
│   │   ├── LedgerController.php
│   │   ├── SyncController.php
│   │   ├── EnglishController.php
│   │   └── ProxyController.php
│   ├── Models/
│   │   ├── TaskModel.php
│   │   ├── CategoryModel.php
│   │   ├── NoteModel.php
│   │   ├── FitnessModel.php
│   │   ├── MediaModel.php
│   │   ├── HealthModel.php
│   │   ├── LedgerModel.php
│   │   └── EnglishModel.php
│   ├── Services/
│   │   ├── SyncService.php
│   │   ├── WeatherProxy.php
│   │   ├── TmdbProxy.php
│   │   └── EnglishProxy.php
│   └── Utils/
│       ├── Logger.php
│       └── Validator.php
├── config/
│   ├── database.php         # DB连接（进Git）
│   ├── api_keys.php         # API Key（不进Git！）
│   └── cors.php
├── sql/
│   └── schema.sql           # 一键建表
├── .htaccess                # Apache重写（如果用Apache）
├── .gitignore
└── README.md
```

### D.2 核心文件示例

#### Response.php

```php
<?php
// src/Core/Response.php
class Response {
    public static function json($data, $httpCode = 200) {
        http_response_code($httpCode);
        header('Content-Type: application/json; charset=utf-8');
        header('Access-Control-Allow-Origin: *');
        header('Access-Control-Allow-Headers: X-Device-ID, Content-Type');
        echo json_encode($data, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT);
        exit;
    }
    
    public static function success($data = null) {
        self::json([
            'code' => 0,
            'message' => 'success',
            'data' => $data,
            'serverTime' => round(microtime(true) * 1000)
        ]);
    }
    
    public static function error($message, $code = 400, $httpCode = 400) {
        self::json(['code' => $code, 'message' => $message], $httpCode);
    }
}
```

#### Auth.php

```php
<?php
// src/Core/Auth.php
class Auth {
    public static function getDeviceId() {
        $deviceId = $_SERVER['HTTP_X_DEVICE_ID'] ?? '';
        
        if (empty($deviceId)) {
            Response::error('Device ID required', 401, 401);
        }
        
        // 可选：验证UUID格式
        if (!preg_match('/^[a-f0-9-]{36}$/i', $deviceId)) {
            Response::error('Invalid Device ID format', 401, 401);
        }
        
        return $deviceId;
    }
}
```

#### Database.php

```php
<?php
// src/Core/Database.php
class Database {
    private static $instance = null;
    private $conn;
    
    private function __construct() {
        $config = require __DIR__ . '/../../config/database.php';
        $dsn = "mysql:host={$config['host']};dbname={$config['dbname']};charset=utf8mb4";
        
        try {
            $this->conn = new PDO($dsn, $config['user'], $config['pass'], [
                PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
                PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
                PDO::ATTR_EMULATE_PREPARES => false,
            ]);
        } catch (PDOException $e) {
            Response::error('Database connection failed', 500, 500);
        }
    }
    
    public static function get() {
        if (self::$instance === null) {
            self::$instance = new self();
        }
        return self::$instance->conn;
    }
}
```

#### database.php（配置示例）

```php
<?php
// config/database.php
return [
    'host' => 'localhost',
    'dbname' => 'selfdiscipline',
    'user' => 'sd_user',
    'pass' => 'your-strong-password',
];
```

> ⚠️ 生产环境密码不要硬编码，可从环境变量读取：`getenv('DB_PASSWORD')`

### D.3 部署检查清单

```
□ 服务器系统更新完毕 (apt update && apt upgrade)
□ Nginx安装并运行
□ PHP 8.2 + php-fpm + php-mysql + php-curl 安装
□ MySQL安装并创建数据库
□ 项目代码上传到 /var/www/selfdiscipline
□ Nginx站点配置写好并启用
□ 域名DNS解析生效 (ping your-domain.com)
□ Let's Encrypt证书申请成功
□ api_keys.php配置好（不进Git）
□ schema.sql导入成功 (mysql < sql/schema.sql)
□ 用curl测试接口通了没
□ Android端改Base URL为 https://your-domain.com/api/v1
□ 真机测试同步流程
```

### D.4 快速测试接口

```bash
# 在服务器上测试
curl http://localhost/api/v1/tasks \
  -H "X-Device-ID: test-uuid-1234-5678" \
  -H "Content-Type: application/json"

# 从外部测试
curl https://your-domain.com/api/v1/tasks \
  -H "X-Device-ID: test-uuid-1234-5678"
```

---

> 📌 **文档结束。下一步建议**：从阶段0开始 → 申请Oracle Cloud免费主机 → 配置环境 → 域名解析 → 然后进入阶段1写第一行代码。
