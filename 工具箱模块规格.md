# 自律工作台 · 独立工具模块规格书

> **版本**：V2.0  
> **日期**：2026-07-30  
> **定位**：8个独立工具模块，各自作为首页可开关的一级板块存在，**不做"工具箱"聚合入口**。每个模块数据独立存储+云端同步，部分模块接入第三方API或AI能力。  
> **V2.0变更**：从V1.x的"工具箱"聚合模式，**拆散为8个独立模块**，每个模块平等地作为首页板块，可单独开关显示/隐藏。

---

## 一、总览

### 1.1 设计理念

之前8个工具被塞进一个"工具箱"入口，需要点两层才能用。现在改为：

> **每个工具都是独立模块，和"任务打卡""笔记系统"平起平坐，首页直接放入口卡片，一步直达。**

### 1.2 模块清单（8个独立模块）

| # | 模块名 | 联网需求 | AI驱动 | 优先级 |
|---|--------|---------|---------|--------|
| 1 | 科学计算器 | ❌ 纯离线 | ❌ | P2 |
| 2 | 单位换算 | ⚠️ 货币需联网 | ❌ | P2 |
| 3 | 二维码工具 | ❌ 纯离线 | ❌ | P2 |
| 4 | 倒数日 | ❌ 纯离线 | ❌ | P2 |
| 5 | 闪卡记忆 | ✅ 必须联网 | ✅ AI生成+自适应复习 | P2 |
| 6 | 习惯打卡 | ✅ 必须联网 | ✅ AI规划+周度复盘 | P2 |
| 7 | 灵感生成器 | ✅ 必须联网 | ✅ AI生成+展开 | P2 |
| 8 | 快递查询 | ✅ 必须联网 | ❌ | P2 |

### 1.3 首页布局（板块化）

```
┌──────────────────────────────────┐
│  📋 任务   📝 笔记   🏃 运动   │
│  📚 英语   🎬 影音   💊 健康   │
│  💰 记账   ☁️ 天气                  │
│  ─── 工具模块（可逐个开关）───    │
│  🔢 计算器 📐 换算   📱 二维码  │
│  ⏳ 倒数日  🃏 闪卡   ✅ 习惯    │
│  💡 灵感   📦 快递               │
└──────────────────────────────────┘
```

每个工具模块在首页占据**一个独立的板块卡片**（和任务/笔记同等地位），用户可以：
- ✅ 在设置页**单独开关**每个模块的显示/隐藏
- ✅ 调整模块在首页的**排列顺序**
- ✅ 长按进入模块主页面

### 1.4 板块配置数据结构

```kotlin
// 每个模块（含工具模块）统一用这套配置
data class ModuleConfig(
    val key: String,           // "task" "calculator" "flashcard" ...
    val displayName: String,    // 显示名
    val iconRes: String,       // 图标
    val isVisible: Boolean,    // 是否显示
    val sortOrder: Int         // 排列顺序
)
```

新增工具模块只需在配置里加一行，首页自动渲染，零侵入。

---

## 二、科学计算器（独立模块）

### 2.1 功能规格

| 功能 | 说明 |
|------|------|
| 四则运算 | + - × ÷，支持括号嵌套 |
| 科学函数 | sin / cos / tan / asin / acos / atan |
| 对数 | log₁₀ / ln / log₂ |
| 幂运算 | x² / xʸ / √x / ³√x / eˣ |
| 常数 | π / e |
| 角度/弧度切换 | 影响三角函数计算结果 |
| 进制转换 | DEC/HEX/BIN/OCT 四种进制互转 |
| 计算历史 | 保存最近100条，可点击回看/复制 |
| 大数显示 | 超长结果可水平滑动查看 |

### 2.2 界面设计

```
┌─────────────────────────────┐
│  ← 科学计算器               │
├─────────────────────────────┤
│                             │
│  计算结果区域（可滑动）       │
│  ┌───────────────────────┐  │
│  │ 3.141592653589793238 │  │
│  └───────────────────────┘  │
│  历史 ▼                      │
│  ┌───────────────────────┐  │
│  │ sin(30°) = 0.5        │  │
│  │ √144 = 12              │  │
│  │ 2^10 = 1024            │  │
│  └───────────────────────┘  │
│                             │
│  [DEG/RAD] [2ⁿd] [AC] [⌫] │
│  [sin] [cos] [tan] [÷]     │
│  [x²] [√x] [log] [×]      │
│  [7] [8] [9] [-]           │
│  [4] [5] [6] [+]           │
│  [1] [2] [3] [=]           │
│  [0] [.] [EXP]             │
│                             │
│  ─── 进制转换 ───          │
│  DEC: 255                  │
│  HEX: FF  BIN: 11111111    │
│  OCT: 377                  │
└─────────────────────────────┘
```

### 2.3 数据模型

```kotlin
@Entity(tableName = "calc_history")
data class CalcHistoryEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val expression: String,        // "sin(30°)"
    val result: String,           // "0.5"
    val mode: Int = 0,            // 0=DEG 1=RAD
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 2.4 同步策略

计算历史走统一同步机制，多设备可共享计算记录。

---

## 三、单位换算（独立模块）

### 3.1 功能规格

| 功能 | 说明 |
|------|------|
| 长度 | 米/千米/厘米/毫米/英寸/英尺/英里/海里 |
| 重量 | 克/千克/吨/斤/磅/盎司 |
| 温度 | 摄氏度/华氏度/开尔文（实时联动） |
| 面积 | 平方米/平方公里/亩/英亩/平方英尺 |
| 体积 | 升/毫升/加仑(美)/加仑(英)/立方英尺 |
| 速度 | km/h / m/s / mph / 节(knots) |
| 时间 | 秒/分/时/天/周/月(30天) |
| 数据存储 | B/KB/MB/GB/TB/PB |
| 货币 | 人民币/美元/欧元/日元/港币/英镑（需联网，汇率API） |
| 输入即转换 | 改任意一栏，全部单位实时联动 |
| 汇率缓存 | 每日更新一次，离线时用缓存 |

### 3.2 界面设计

```
┌─────────────────────────────┐
│  ← 单位换算                 │
├─────────────────────────────┤
│                             │
│  选择类别：[长度▼]           │
│                             │
│  ┌───────────────────────┐  │
│  │ 1                     │  │ ← 输入
│  │ 千米(km)              │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 1000                  │  │ ← 自动换算
│  │ 米(m)                 │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 0.621                 │  │
│  │ 英里(mi)              │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 3280.84               │  │
│  │ 英尺(ft)              │  │
│  └───────────────────────┘  │
│                             │
│  [+ 添加更多单位]           │
│                             │
│  💱 货币汇率更新于 2小时前  │
└─────────────────────────────┘
```

### 3.3 数据模型

```kotlin
@Entity(tableName = "unit_conversion_cache")
data class UnitCacheEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val baseCurrency: String = "CNY",
    val ratesJson: String,        // JSON: {"USD":7.2,"EUR":7.8}
    val cachedAt: Long,           // 缓存时间
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 3.4 汇率API

```php
// GET /api/currency/rates?base=CNY
// 返回 {"base":"CNY","rates":{"USD":0.14,"EUR":0.13},"updated_at":...}

public function getRates() {
    $base = $_GET['base'] ?? 'CNY';
    $config = require __DIR__ . '/../../config/api_keys.php';
    
    // 优先返回缓存（30分钟内不重复请求）
    $cacheFile = "/tmp/rates_{$base}.json";
    if (file_exists($cacheFile) && time() - filemtime($cacheFile) < 1800) {
        Response::success(json_decode(file_get_contents($cacheFile), true));
    }
    
    // 调用 exchangerate-api.com（免费1000次/月）
    $apiKey = $config['exchangerate_key'];
    $url = "https://v6.exchangerate-api.com/v6/{$apiKey}/latest/{$base}";
    $result = json_decode(file_get_contents($url), true);
    
    file_put_contents($cacheFile, json_encode($result));
    Response::success($result);
}
```

---

## 四、二维码工具（独立模块）

### 4.1 功能规格

| 功能 | 说明 |
|------|------|
| 生成二维码 | 输入文字/网址/WiFi密码 → 生成二维码图片 |
| 保存到相册 | 长按/按钮保存生成的二维码 |
| 扫码识别 | CameraX扫码 → 自动识别内容 |
| URL自动打开 | 扫到网址自动弹出"是否在浏览器打开" |
| WiFi自动连接 | 扫到WiFi二维码自动填充网络配置 |
| 文本复制 | 扫到文本内容一键复制 |
| 扫码历史 | 保存最近50条扫码记录 |
| 生成类型 | 支持：文本/网址/WiFi/vCard名片/短信/电话 |

### 4.2 界面设计

```
┌─────────────────────────────┐
│  ← 二维码工具    [📷扫码]  │
├─────────────────────────────┤
│                             │
│  [生成] [扫码]  ← 模式切换  │
│                             │
│  ── 生成模式 ──            │
│  类型：[网址▼]               │
│  ┌───────────────────────┐  │
│  │ https://example.com   │  │
│  └───────────────────────┘  │
│       [生成二维码]            │
│                             │
│   ┌─────────────────────┐   │
│   │                     │   │
│   │   ████  ██  ████  │   │
│   │   █  █  █  █  █   │   │  ← 二维码图片
│   │   ████  ██  ████  │   │
│   │   █  █  ██  █  █  │   │
│   │   ████  ████  ████  │   │
│   │                     │   │
│   └─────────────────────┘   │
│       [💾 保存到相册]        │
│                             │
│  ── 扫码模式 ──            │
│                             │
│   ┌─────────────────────┐   │
│   │                     │   │
│   │   📷 对准二维码      │   │
│   │   自动识别中...      │   │
│   │                     │   │
│   └─────────────────────┘   │
│                             │
│  识别结果：https://...       │
│  [打开链接] [复制]           │
│                             │
│  📋 扫码历史 (5)            │
└─────────────────────────────┘
```

### 4.3 数据模型

```kotlin
@Entity(tableName = "qr_scan_history")
data class QrScanHistoryEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val content: String,           // 扫码内容
    val type: Int,                 // 0=文本 1=URL 2=WiFi 3=vCard 4=SMS 5=电话
    val scannedAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 4.4 技术选型

| 功能 | 库 |
|------|-----|
| 生成二维码 | ZXing (`com.journeyapps:zxing-android-embedded`) |
| 扫码 | CameraX + ZXing / ML Kit Barcode Scanning |
| 图片保存 | MediaStore API |

---

## 五、倒数日（独立模块）

### 5.1 功能规格

| 功能 | 说明 |
|------|------|
| 新建事件 | 标题 + 目标日期 + 模式（倒数/正数） |
| 大数字倒计时 | 首页大字体显示"距离考研还有 128 天" |
| 自定义背景 | 每个事件可设背景图/纯色 |
| 分类标签 | 考试/生日/旅行/恋爱/自定义 |
| 通知提醒 | 到达当天推送"今天就是D-Day！" |
| 年度循环 | 每年同一天自动重新倒数（生日/纪念日） |
| 过期保留 | 过了的日子保留为"已过去XX天" |
| 置顶 | 可置顶最重要的倒数日 |

### 5.2 界面设计

```
┌─────────────────────────────┐
│  ← 倒数日        [+新建]    │
├─────────────────────────────┤
│                             │
│  ┌───────────────────────┐  │
│  │ 🎓 考研              │  │  ← 置顶
│  │ 背景图（用户上传）     │  │
│  │                       │  │
│  │    还有 128 天        │  │  ← 大数字
│  │    2026-12-20         │  │
│  │    📚 考试             │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 🎂 妈妈生日           │  │
│  │ 纯色背景 #FF6B6B      │  │
│  │    还有 45 天          │  │
│  │    🔁 每年循环         │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ ✈️ 日本旅行            │  │
│  │    已过 12 天          │  │  ← 正数模式
│  │  "回来后第12天"       │  │
│  └───────────────────────┘  │
│                             │
│  [+ 新建倒数日]            │
└─────────────────────────────┘

新建弹窗：
┌──────────────────────┐
│  新建倒数日            │
├──────────────────────┤
│ 标题：[考研]          │
│ 日期：[2026-12-20 📅] │
│ 模式：(●)倒数 ( )正数 │
│ 分类：[考试▼]         │
│ 背景：[🖼️ 选图片]    │
│ 提醒：[✓]当天提醒     │
│ 循环：[ ]每年重复     │
│                      │
│   [取消]  [创建]      │
└──────────────────────┘
```

### 5.3 数据模型

```kotlin
@Entity(tableName = "countdown_events")
data class CountdownEventEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val title: String,
    val targetDate: Long,         // 目标日期（毫秒）
    val mode: Int,                // 0=倒数 1=正数
    val category: String,         // "exam" "birthday" "travel" "love" "custom"
    val backgroundType: Int = 0,  // 0=纯色 1=图片
    val backgroundColor: String = "#4A90D9",
    val backgroundImagePath: String = "",
    val reminderEnabled: Boolean = false,
    val reminderTime: Long = 0,   // 提醒时间
    val repeatYearly: Boolean = false,
    val isPinned: Boolean = false,
    val isExpired: Boolean = false,
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 5.4 通知提醒

复用现有的 `ReminderScheduler`（和健康复诊提醒同一套AlarmManager机制）：

```kotlin
// 创建倒数日时
if (event.reminderEnabled) {
    reminderScheduler.schedule(
        id = event.id.hashCode(),
        triggerAt = event.targetDate,
        title = "⏳ ${event.title}",
        content = "今天就是D-Day！加油！"
    )
}
```

### 5.5 同步策略

倒数日数据走统一同步机制，换设备后倒数日自动恢复。

---

## 六、闪卡记忆（独立模块 · 纯AI驱动）

### 6.1 功能规格

| 功能 | 说明 |
|------|------|
| AI生成卡片 | 输入主题/关键词 → AI自动生成10~30张闪卡 |
| 难度分级 | AI根据内容自动标注难度（简单/中等/困难） |
| 翻卡学习 | 点击卡片翻转，正面→反面 |
| 自适应复习 | AI根据答题评分动态调整下次出题策略 |
| 学习统计 | 今日已学/待复习/掌握率/连续学习天数 |
| 卡组管理 | 按主题分组，每个卡组由一次AI生成产生 |
| 联网依赖 | **必须联网**，无网络时显示友好提示 |

> ⚠️ **100%由AI生成**，不做手动创建、不做本地内置内容。

### 6.2 AI生成流程

```
用户输入："帮我生成15张关于JavaScript闭包的闪卡，难度从易到难"
    │
    ▼
App调用 /api/ai/flashcards (PHP代理)
    │
    ▼
PHP调用AI API（OpenAI/通义千问/文心一言，可切换）
    │
    ▼
AI返回JSON：
[
  {"front": "什么是闭包？", "back": "函数及其词法环境的组合...", "difficulty": 1},
  {"front": "以下代码输出什么？...", "back": "输出undefined，因为...", "difficulty": 3},
  ...
]
    │
    ▼
App批量创建卡片 → 自动进入学习模式
```

### 6.3 AI自适应复习

```
用户学习一张卡 → 自评掌握程度(1~5星)
    │
    ▼
App将评分 + 卡片难度 + 历史答题 发给 /api/ai/flashcard-review
    │
    ▼
AI返回：
{
  "next_review_in_days": 3,
  "should_repeat_today": false,
  "hint": "这道题涉及作用域链，建议结合画图理解"
}
    │
    ▼
App更新该卡片的nextReviewDate
```

### 6.4 界面设计

```
┌─────────────────────────────┐
│  ← 闪卡记忆    📊统计       │
├─────────────────────────────┤
│  [全部] [JS] [英语] [历史]  │
│                             │
│   ┌─────────────────────┐   │
│   │  🤖 AI生成 · JS闭包  │  │
│   │                     │   │
│   │   以下代码输出什么？ │   │  ← 正面
│   │   function outer(){  │   │
│   │     let x=1;        │   │
│   │     return function(){│   │
│   │       console.log(x) │   │
│   │     }               │   │
│   │   }                 │   │
│   │   outer()()         │   │
│   │                     │   │
│   │   ┌───────────┐    │   │
│   │   │  点击看答案 │    │   │
│   │   └───────────┘    │   │
│   └─────────────────────┘   │
│                             │
│  我觉得：                     │
│  [太简单] [刚好] [太难]     │
│  ★☆☆☆☆  ★★☆☆☆  ★★★☆☆  │
│                             │
│  [上一张]    [下一张]       │
│                             │
│  ─────────────────────     │
│  🤖 让AI生成新卡组           │
│  ┌───────────────────────┐  │
│  │ 输入主题，如"考研政治"  │  │
│  └───────────────────────┘  │
│  [生成10张] [生成20张]      │
│                             │
│  📶 需要联网  ✓已连接       │
└─────────────────────────────┘

断网状态：
┌─────────────────────────────┐
│  ⚠️ 闪卡需要联网             │
│                             │
│   当前没有网络连接            │
│   AI闪卡功能暂不可用         │
│                             │
│   你可以查看已有卡组并复习  │
│                             │
│  [重试]                     │
└─────────────────────────────┘
```

### 6.5 数据模型

```kotlin
@Entity(tableName = "flashcards")
data class FlashcardEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val deckId: String,
    val front: String,
    val back: String,
    val difficulty: Int = 2,        // 1=简单 2=中等 3=困难
    val familiarity: Int = 1,       // 用户自评 1-5
    val easeFactor: Double = 2.5,   // SM-2难度系数
    val intervalDays: Int = 1,
    val nextReviewDate: Long,
    val reviewCount: Int = 0,
    val correctCount: Int = 0,
    val aiHint: String = "",
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()

@Entity(tableName = "flashcard_decks")
data class FlashcardDeckEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val name: String,
    val description: String,
    val topic: String,               // 用户输入的主题
    val cardCount: Int = 0,
    val difficultyRange: String = "1-3",
    val aiProvider: String = "",
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 6.6 AI接口

#### 6.6.1 生成闪卡

```php
// POST /api/ai/flashcards
// 输入：{"topic": "JavaScript闭包", "count": 15, "difficulty": "1-3"}
// 输出：{"deck_name": "JS闭包专项", "cards": [{"front":"...","back":"...","difficulty":1}]}

public function generateFlashcards() {
    $input = json_decode(file_get_contents('php://input'), true);
    $topic = $input['topic'] ?? '';
    $count = min(intval($input['count'] ?? 10), 30);
    $difficulty = $input['difficulty'] ?? '1-3';
    
    $aiConfig = require __DIR__ . '/../../config/ai_keys.php';
    $provider = $aiConfig['provider'];
    
    $prompt = "你是一个专业的闪卡生成器。请生成{$count}张关于「{$topic}」的闪卡。\n";
    $prompt .= "难度范围：{$difficulty}（1=简单 2=中等 3=困难）\n";
    $prompt .= "要求：\n";
    $prompt .= "1. 正面是问题或代码片段，反面是详细答案\n";
    $prompt .= "2. 难度从易到难排列\n";
    $prompt .= "3. 包含概念理解、代码分析、实际应用三种类型\n";
    $prompt .= '严格返回JSON：{"deck_name":"...","cards":[{"front":"...","back":"...","difficulty":1}]}';
    $prompt .= "不要输出任何JSON以外的内容";
    
    $result = $this->callAiProvider($provider, $prompt);
    $data = json_decode($result, true);
    
    if (!isset($data['cards']) || !is_array($data['cards'])) {
        $data = $this->fallbackCards($topic, $count);
    }
    
    Response::success($data);
}
```

#### 6.6.2 自适应复习建议

```php
// POST /api/ai/flashcard-review
// 输入：{"card_id":"xxx","difficulty":2,"user_rating":4,"history":[1,1,0,1]}
// 输出：{"next_review_in_days":3,"should_repeat_today":false,"hint":"..."}

public function reviewSuggestion() {
    $input = json_decode(file_get_contents('php://input'), true);
    // AI根据用户表现动态调整复习策略
    // ...
}
```

### 6.7 断网处理

| 场景 | 行为 |
|------|------|
| 打开闪卡页，无网络 | 显示断网提示页，已有卡组可继续复习 |
| 学习中突然断网 | 允许完成当前卡片，下一个操作提示联网 |
| 有缓存卡组但已过期 | 不显示旧内容，引导联网刷新 |

---

## 七、习惯打卡（独立模块 · 纯AI驱动）

### 7.1 功能规格

| 功能 | 说明 |
|------|------|
| AI生成习惯清单 | 输入目标 → AI生成3~5个关联习惯，一键创建 |
| 每日打卡 | 一键标记今日完成 |
| 连续天数 | 自动计算当前连续打卡天数 |
| 热力图 | GitHub风格贡献图（绿/深绿/更深绿） |
| 习惯统计 | 本周完成率、本月完成率、最长连续记录 |
| AI周度复盘 | 每周AI分析打卡数据，建议"加码/减负/替换" |
| AI智能提醒 | AI根据打卡历史推荐最佳提醒时间 |
| 打卡备注 | 可选，每天打卡时可写一句话 |
| 联网依赖 | **必须联网**，无网络时只展示已有习惯 |

> ⚠️ **100%由AI生成**，不做手动创建、不做本地推荐列表。

### 7.2 与任务模块的区别

| | 任务模块 | 习惯打卡 |
|---|---|---|
| 本质 | "今天必须做完这件事" | "每天重复一个小动作" |
| 粒度 | 大（备考、写报告） | 小（喝8杯水、早睡、冥想5min） |
| 时间 | 有截止日期 | 无固定期限，长期养成 |
| 来源 | 用户自己定 | **AI根据目标生成** |

### 7.3 AI交互流程

#### 首次创建习惯

```
用户输入："我想提高专注力"
    │
    ▼
调用 POST /api/ai/habit-plan
    │
    ▼
AI返回：
{
  "plan_name": "21天专注力提升计划",
  "habits": [
    {"name":"每天番茄钟25min","icon":"🍅","frequency":0,"reminder_hour":9},
    {"name":"手机勿扰1小时","icon":"📵","frequency":0,"reminder_hour":14},
    {"name":"晨间冥想10min","icon":"🧘","frequency":0,"reminder_hour":7},
    {"name":"睡前阅读30min","icon":"📖","frequency":0,"reminder_hour":22}
  ]
}
    │
    ▼
App展示AI推荐 → 用户勾选 → 一键创建
```

#### 每周AI复盘

```
每周一 08:00 自动触发
    │
    ▼
App收集上周打卡数据 → POST /api/ai/habit-review
    │
    ▼
AI返回：
{
  "insights": [
    "🧘 冥想完成率100%，可以加码到15分钟了",
    "📵 手机勿扰完成率仅30%，建议改到晚上做"
  ],
  "suggestions": [
    {"action":"increase","habit":"冥想","new_value":"15min"},
    {"action":"reschedule","habit":"手机勿扰","new_time":"21:00"},
    {"action":"add","habit":{"name":"午后散步10min","icon":"🚶","frequency":1}}
  ]
}
    │
    ▼
App展示建议 → 用户确认后应用
```

### 7.4 界面设计

```
┌─────────────────────────────┐
│  ← 习惯打卡                 │
├─────────────────────────────┤
│                             │
│  💧 每天喝8杯水     ✅     │
│     连续 12 天  🔥            │
│                             │
│  🌙 23:00前睡觉    ⬜     │
│     连续 5 天                │
│                             │
│  🧘 冥想5分钟      ✅     │
│     连续 30 天  🔥🔥🔥      │
│                             │
│  ───────────────────────    │
│  🤖 AI习惯规划               │
│  "想养成什么习惯？输入目标"  │
│  ┌───────────────────────┐  │
│  │ 我想提高专注力         │  │
│  └───────────────────────┘  │
│  [🤖 让AI规划]              │
│                             │
│  → 🍅 每天番茄钟25min  ✓   │
│  → 📵 手机勿扰1小时   ✓   │
│  → 🧘 晨间冥想10min   ✓   │
│         [全部创建]            │
│                             │
│  ───────────────────────    │
│  📊 本周AI复盘              │
│  🧘 冥想100% → 加码15min   │
│  📵 勿扰30% → 改到晚上    │
│  [应用建议]  [忽略]          │
│                             │
│  ───────────────────────    │
│  📊 我的热力图              │
│  ┌─┬─┬─┬─┬─┬─┬─┐          │
│  │🟢│🟩│🟩│⬜│🟢│🟩│🟢│  │
│  ├─┼─┼─┼─┼─┼─┼─┤          │
│  │🟩│🟢│🟩│🟩│🟢│🟩│🟩│  │
│  └─┴─┴─┴─┴─┴─┴─┘          │
│                             │
│  📶 需要联网  ✓已连接       │
└─────────────────────────────┘
```

### 7.5 数据模型

```kotlin
@Entity(tableName = "habit_plans")
data class HabitPlanEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val goal: String,               // "提高专注力"
    val planName: String,          // "21天专注力提升计划"
    val aiProvider: String = "",
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()

@Entity(tableName = "habits")
data class HabitEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val planId: String,
    val name: String,
    val icon: String,
    val color: String,
    val frequency: Int,             // 0=每天 1=工作日 2=周末 3=自定义
    val customDays: String = "",
    val reminderHour: Int = -1,
    val reminderMinute: Int = 0,
    val currentStreak: Int = 0,
    val longestStreak: Int = 0,
    val totalCheckins: Int = 0,
    val aiNotes: String = "",
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()

@Entity(tableName = "habit_checkins")
data class HabitCheckinEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val habitId: String,
    val date: String,                // "2026-07-30"
    val note: String = "",
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 7.6 AI接口

#### 7.6.1 生成习惯计划

```php
// POST /api/ai/habit-plan
// 输入：{"goal": "我想提高专注力", "count": 4}
// 输出：{"plan_name":"...","habits":[{"name":"...","icon":"🍅","frequency":0,"reminder_hour":9}]}

public function generateHabitPlan() {
    $input = json_decode(file_get_contents('php://input'), true);
    $goal = $input['goal'] ?? '';
    $count = min(intval($input['count'] ?? 4), 6);
    
    $prompt = "你是习惯养成专家。用户目标：「{$goal}」\n";
    $prompt .= "请生成{$count}个具体、可量化、循序渐进的微习惯。\n";
    $prompt .= "规则：每个习惯必须非常小（2-15分钟），习惯之间要有关联性。\n";
    $prompt .= '严格JSON：{"plan_name":"...","habits":[{"name":"...","icon":"🍅","frequency":0,"reminder_hour":9}]}';
    
    $result = $this->callAiProvider($this->getProvider(), $prompt);
    // 校验 + 返回
}
```

#### 7.6.2 周度复盘

```php
// POST /api/ai/habit-review
// 输入：{"plan_id":"xxx","week_data":[{"habit":"冥想","rate":1.0}]}
// 输出：{"insights":["..."],"suggestions":[{"action":"increase","habit":"冥想","new_value":"15min"}]}

public function weeklyReview() {
    // AI分析 → 返回洞察 + 建议
}
```

### 7.7 断网处理

| 场景 | 行为 |
|------|------|
| 打开习惯页，无网络 | 显示已有习惯 + 打卡正常，隐藏"AI规划"入口 |
| 点击"AI规划"，无网络 | Toast提示"需要联网" |
| 每周复盘触发，无网络 | 跳过，下周合并 |

---

## 八、灵感生成器（独立模块 · 纯AI驱动）

### 8.1 功能规格

| 功能 | 说明 |
|------|------|
| AI生成灵感 | 选分类+风格 → AI生成3~5个个性化创作主题 |
| 分类筛选 | 写作 / 绘画 / 编程 / 摄影 / 设计 / 音乐 / 视频 |
| 风格指定 | 科幻/悬疑/治愈/黑色幽默/赛博朋克… |
| 收藏夹 | 喜欢的灵感可以收藏，随时翻看 |
| 历史记录 | 每次生成的灵感都保存 |
| AI二次展开 | 对某个灵感点"展开"，AI帮你扩展成大纲+开头 |
| 一键转笔记 | 生成的灵感直接创建为笔记草稿 |
| 联网依赖 | **必须联网**，无网络时显示友好提示 |

> ⚠️ **100%由AI生成**，不做本地内置灵感库。

### 8.2 AI生成流程

```
用户选择：分类=写作，风格=科幻+悬疑
    │
    ▼
调用 POST /api/ai/inspiration
    │
    ▼
AI返回：
{
  "items": [
    {"content":"外星考古学家发现一座地球教堂，祭坛上放着写你名字的纸条。以此为开头写2000字。","category":"writing"},
    {"content":"写一个侦探故事，唯一线索是被剪切的监控视频，画面里时间在倒流。","category":"writing"}
  ]
}
    │
    ▼
App展示灵感卡片 → 可收藏/转笔记/展开
```

### 8.3 AI二次展开

```
用户点击"展开" → POST /api/ai/inspiration-expand
    │
    ▼
AI返回：
{
  "outline": "一、守墓人发现异常...二、追溯信号...三、真相揭晓...",
  "opening": "第38742天，守墓人AI照常巡检。光学传感器捕捉到地窖深处一束不该存在的光...",
  "tips": "建议用第一人称AI视角，制造疏离感和诡异感"
}
```

### 8.4 界面设计

```
┌─────────────────────────────┐
│  ← 灵感生成器               │
├─────────────────────────────┤
│ 分类：[写作▼][绘画][编程]... │
│ 风格：[科幻][悬疑][+自定义]  │
│                             │
│  ┌───────────────────────┐  │
│  │ ✨ 灵感 1              │  │
│  │ 外星考古学家在一颗死星 │  │
│  │ 上发现一座地球教堂…    │  │
│  │                       │  │
│  │ [📝转笔记][🔄展开][⭐收藏]│
│  ├───────────────────────┤  │
│  │ ✨ 灵感 2              │  │
│  │ 写一个侦探故事…        │  │
│  │ [📝转笔记][🔄展开][⭐收藏]│
│  └───────────────────────┘  │
│                             │
│    [🎲 再来一批]            │
│                             │
│  ⭐ 收藏夹 (12)  📋 历史    │
│                             │
│  📶 需要联网  ✓已连接       │
└─────────────────────────────┘
```

### 8.5 数据模型

```kotlin
@Entity(tableName = "inspiration_items")
data class InspirationItemEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val content: String,
    val category: String,          // "writing""painting""coding"...
    val style: String = "",       // "sci-fi""mystery"...
    val outline: String = "",     // AI展开后的大纲
    val opening: String = "",     // AI展开后的开头
    val tips: String = "",        // AI创作建议
    val isFavorite: Boolean = false,
    val generatedBy: String = "",
    val createdAt: Long = System.currentTimeMillis(),
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 8.6 AI接口

```php
// POST /api/ai/inspiration
// 输入：{"category":"writing","style":"sci-fi,mystery","count":3}
// 输出：{"items":[{"content":"...","category":"writing"}]}

public function generate() {
    $input = json_decode(file_get_contents('php://input'), true);
    $category = $input['category'] ?? 'writing';
    $style = $input['style'] ?? '';
    $count = min(intval($input['count'] ?? 3), 10);
    
    $prompt = "你是一个创意灵感生成器。生成{$count}个「{$category}」的创作主题。\n";
    if ($style) $prompt .= "风格偏向：{$style}\n";
    $prompt .= "要求：每个灵感50-100字，有画面感和故事性，追求新颖。\n";
    $prompt .= '严格JSON：{"items":[{"content":"...","category":"'.$category.'"}]}';
    
    $result = $this->callAiProvider($this->getProvider(), $prompt);
    // 校验 + 返回
}

// POST /api/ai/inspiration-expand
// 输入：{"content":"...","category":"writing"}
// 输出：{"outline":"...","opening":"...","tips":"..."}
```

### 8.7 断网处理

| 场景 | 行为 |
|------|------|
| 打开灵感页，无网络 | 隐藏生成按钮，只展示收藏夹和历史 |
| 点"再来一批"，无网络 | Toast提示"需要联网" |
| 有缓存收藏/历史 | 正常展示 |

---

## 九、快递查询（独立模块）

### 9.1 功能规格

| 功能 | 说明 |
|------|------|
| 输入单号查询 | 手动输入 → 自动识别快递公司 → 返回物流轨迹 |
| 扫码查快递 | 扫快递单条形码/二维码自动填入 |
| 公司识别 | 根据单号前缀自动判断（顺丰/中通/圆通/韵达/京东…） |
| 物流轨迹 | 时间 + 地点 + 状态（已揽收/运输中/派送中/已签收） |
| 签收提醒 | 状态变为"已签收"时推送通知 |
| 多单管理 | 保存所有快递单号，列表展示状态摘要 |
| 手动刷新 | 下拉刷新获取最新物流 |

### 9.2 快递API选型

| API | 免费额度 | 推荐度 |
|-----|---------|--------|
| **快递100** | 100次/天 | ⭐⭐⭐⭐⭐ |
| 快递鸟 | 100次/天 | ⭐⭐⭐⭐ |
| 阿里云快递查询 | 按量付费 | ⭐⭐⭐ |

### 9.3 界面设计

```
┌─────────────────────────────┐
│  ← 快递查询      [📷扫码]   │
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ 输入快递单号        📷  │  │
│  └───────────────────────┘  │
│       [查询]                │
│                             │
│  我的快递 (3)               │
│  ┌───────────────────────┐  │
│  │ 📦 顺丰速运             │  │
│  │ SF1234567890           │  │
│  │ 运输中 → 邢台市        │  │
│  │ 更新于 2小时前          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 📦 中通快递             │  │
│  │ ZT9876543210           │  │
│  │ ✅ 已签收 07-29 14:30  │  │
│  └───────────────────────┘  │
│                             │
│  物流详情（点击展开）        │
│  ┌───────────────────────┐  │
│  │ 07-30 10:15 运输中    │  │
│  │   到达 邢台转运中心    │  │
│  │ 07-29 20:30 运输中    │  │
│  │   已离开 广州市中心    │  │
│  │ 07-29 18:00 已揽收    │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 9.4 数据模型

```kotlin
@Entity(tableName = "express_packages")
data class ExpressPackageEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val trackingNumber: String,
    val companyCode: String,
    val companyName: String,
    val status: Int,               // 0=待揽收 1=运输中 2=派送中 3=已签收 4=异常
    val statusText: String,
    val lastUpdate: Long,
    val estimatedDelivery: String = "",
    val rawData: String = "",
    override val lastModified: Long = System.currentTimeMillis(),
    override val isDeleted: Boolean = false,
    override val needsSync: Boolean = false
) : BaseEntity()
```

### 9.5 PHP代理实现

```php
// GET /api/express/query?number=SF1234567890
public function query() {
    $number = $_GET['number'] ?? '';
    if (empty($number)) Response::error('单号不能为空');
    
    $config = require __DIR__ . '/../../config/api_keys.php';
    $apiKey = $config['kuaidi100_key'];
    $apiSecret = $config['kuaidi100_secret'];
    
    // 1. 自动识别快递公司
    $company = $this->detectCompany($number);
    
    // 2. 调用快递100 API
    // 3. 返回格式化结果
    Response::success($result);
}

private function detectCompany($number) {
    if (preg_match('/^SF/i', $number)) return 'shunfeng';
    if (preg_match('/^(YT|DY)/i', $number)) return 'yuantong';
    // ...
    return '';
}
```

### 9.6 通知提醒

```kotlin
// 快递状态变为"已签收"时
if (newStatus == 3 && oldStatus != 3) {
    notificationManager.notify(
        title = "📦 ${express.companyName}",
        content = "${express.trackingNumber} 已签收！"
    )
}
```

---

## 十、AI接入统一设计

### 10.1 架构

```
Android App（8个独立模块）
    │
    ▼
PHP后端 /api/ai/* (统一代理)
    │
    ▼
AI服务商API（可切换）
    ├── OpenAI GPT (国外)
    ├── 通义千问 (阿里，默认)
    ├── 文心一言 (百度)
    └── 智谱GLM (清华)
```

### 10.2 配置

```php
// config/ai_keys.php（不进Git）
<?php
return [
    'provider' => 'qwen',
    'daily_limit' => 100,    // 每日AI调用上限
    'providers' => [
        'openai' => ['api_key' => 'sk-xxxx', 'base_url' => 'https://api.openai.com/v1'],
        'qwen' => ['api_key' => 'sk-xxxx', 'base_url' => 'https://dashscope.aliyuncs.com/api/v1'],
        'wenxin' => ['api_key' => 'xxxx', 'secret_key' => 'xxxx']
    ]
];
```

### 10.3 AI接口清单（6个）

| 接口 | URL | 消费方 |
|------|-----|--------|
| 生成闪卡 | POST /api/ai/flashcards | 闪卡记忆模块 |
| 复习建议 | POST /api/ai/flashcard-review | 闪卡记忆模块 |
| 生成习惯计划 | POST /api/ai/habit-plan | 习惯打卡模块 |
| 周度复盘 | POST /api/ai/habit-review | 习惯打卡模块 |
| 生成灵感 | POST /api/ai/inspiration | 灵感生成器模块 |
| 展开灵感 | POST /api/ai/inspiration-expand | 灵感生成器模块 |

### 10.4 断网/降级统一策略

| 模块 | 断网行为 |
|------|----------|
| 闪卡记忆 | 显示"需要联网"，已有卡组可复习 |
| 习惯打卡 | 显示"需要联网"，已有习惯可打卡 |
| 灵感生成器 | 显示"需要联网"，收藏夹和历史可查看 |

> 后端设 `daily_limit` 统一限流，超限返回429，App展示"今日AI额度已用完，明天再来 🌙"。

---

## 十一、数据库新增表汇总

| 表名 | 用途 | 所属模块 | 同步 |
|------|------|---------|------|
| calc_history | 计算器历史 | 科学计算器 | ✅ |
| unit_conversion_cache | 汇率缓存 | 单位换算 | ✅ |
| qr_scan_history | 扫码历史 | 二维码工具 | ✅ |
| countdown_events | 倒数日事件 | 倒数日 | ✅ |
| habit_plans | 习惯计划 | 习惯打卡 | ✅ |
| habits | 习惯定义 | 习惯打卡 | ✅ |
| habit_checkins | 习惯打卡记录 | 习惯打卡 | ✅ |
| flashcards | 闪卡 | 闪卡记忆 | ✅ |
| flashcard_decks | 闪卡卡组 | 闪卡记忆 | ✅ |
| inspiration_items | 灵感 | 灵感生成器 | ✅ |
| express_packages | 快递单号 | 快递查询 | ✅ |

---

## 十二、后端新增接口汇总

| 方法 | URL | 说明 | 所属模块 |
|------|-----|------|---------|
| GET | /api/currency/rates?base=CNY | 汇率查询 | 单位换算 |
| GET | /api/express/query?number=xxx | 快递查询 | 快递查询 |
| GET | /api/express/companies | 快递公司列表 | 快递查询 |
| POST | /api/ai/flashcards | AI生成闪卡 | 闪卡记忆 |
| POST | /api/ai/flashcard-review | AI复习建议 | 闪卡记忆 |
| POST | /api/ai/habit-plan | AI生成习惯计划 | 习惯打卡 |
| POST | /api/ai/habit-review | AI周度复盘 | 习惯打卡 |
| POST | /api/ai/inspiration | AI生成灵感 | 灵感生成器 |
| POST | /api/ai/inspiration-expand | AI展开灵感 | 灵感生成器 |
| GET | /api/ai/status | 今日AI额度查询 | 通用 |

---

## 十三、Android端分包结构（新增部分）

```
com.yourpackage.selfdiscipline/
├── feature/
│   ├── calculator/          # 科学计算器模块
│   │   ├── CalcActivity.kt
│   │   ├── CalcViewModel.kt
│   │   ├── CalcHistoryDao.kt
│   │   └── CalcHistoryEntity.kt
│   ├── converter/          # 单位换算模块
│   │   ├── ConverterActivity.kt
│   │   ├── ConverterViewModel.kt
│   │   └── CurrencyRepository.kt
│   ├── qrcode/            # 二维码工具模块
│   │   ├── QrActivity.kt
│   │   ├── QrGenerateFragment.kt
│   │   ├── QrScanFragment.kt
│   │   └── QrScanDao.kt
│   ├── countdown/         # 倒数日模块
│   │   ├── CountdownActivity.kt
│   │   ├── CountdownViewModel.kt
│   │   ├── CountdownDao.kt
│   │   └── CountdownEntity.kt
│   ├── flashcard/         # 闪卡记忆模块
│   │   ├── FlashcardActivity.kt
│   │   ├── StudyFragment.kt
│   │   ├── FlashcardViewModel.kt
│   │   ├── FlashcardDao.kt
│   │   └── DeckDao.kt
│   ├── habit/             # 习惯打卡模块
│   │   ├── HabitActivity.kt
│   │   ├── HabitViewModel.kt
│   │   ├── HabitDao.kt
│   │   ├── CheckinDao.kt
│   │   └── PlanDao.kt
│   ├── inspiration/       # 灵感生成器模块
│   │   ├── InspirationActivity.kt
│   │   ├── InspirationViewModel.kt
│   │   └── InspirationDao.kt
│   └── express/           # 快递查询模块
│       ├── ExpressActivity.kt
│       ├── ExpressViewModel.kt
│       └── ExpressDao.kt
```

---

## 十四、PHP后端分包结构（新增部分）

```
src/Controllers/
├── CalculatorController.php    # 计算器历史CRUD
├── ConverterController.php    # 汇率代理
├── QrCodeController.php       # 扫码历史CRUD
├── CountdownController.php    # 倒数日CRUD
├── FlashcardController.php    # 闪卡+卡组CRUD
├── HabitController.php        # 习惯+打卡CRUD
├── InspirationController.php   # 灵感CRUD
├── ExpressController.php      # 快递查询+历史
└── AiController.php           # AI统一代理（6个接口）

src/Models/
├── CalcHistoryModel.php
├── UnitCacheModel.php
├── QrScanModel.php
├── CountdownModel.php
├── FlashcardModel.php
├── FlashcardDeckModel.php
├── HabitModel.php
├── HabitCheckinModel.php
├── HabitPlanModel.php
├── InspirationModel.php
└── ExpressModel.php
```

---

## 十五、开发任务拆解

| 任务 | 工期 | 所属模块 | 依赖 |
|------|------|---------|------|
| AI统一接入层（PHP代理+限流+降级） | 2天 | 通用 | AI服务申请 |
| 科学计算器（含进制转换） | 2天 | 计算器 | 无 |
| 单位换算（7类+货币） | 2天 | 单位换算 | 汇率API |
| 二维码生成+扫码 | 2天 | 二维码 | ZXing集成 |
| 倒数日（CRUD+通知+背景图） | 2天 | 倒数日 | 复用ReminderScheduler |
| 闪卡记忆（AI生成+SM-2复习） | 3天 | 闪卡 | AI接口 |
| 闪卡自适应复习 | 1天 | 闪卡 | AI接口 |
| 习惯打卡（AI生成+热力图） | 3天 | 习惯 | AI接口 |
| 习惯周度复盘 | 1天 | 习惯 | AI接口 |
| 灵感生成器（AI生成+展开） | 2天 | 灵感 | AI接口 |
| 快递查询（扫码+查询+提醒） | 2天 | 快递 | 快递100API |
| 首页板块开关+排序配置 | 1天 | 通用 | 无 |
| 全部同步接入 | 1天 | 通用 | 各模块完成 |
| **合计** | **22天** | | |

---

## 十六、风险与应对

| 风险 | 应对 |
|------|------|
| AI API费用超支 | 后端设每日调用上限(daily_limit)，超限返回429 |
| AI返回内容质量差 | 后端做JSON格式校验+内容长度校验，异常时返回备用内容 |
| AI服务宕机/超时 | PHP设5秒超时，失败返回备用内容+状态码 |
| 闪卡/习惯/灵感完全依赖网络 | 已有数据可离线查看，断网UI明确引导 |
| 快递API限流 | 本地缓存每个单号结果30分钟 |
| 汇率API挂了 | 用上次缓存汇率，显示"汇率可能过期" |
| ZXing扫码性能 | 复杂背景下识别慢，降级用ML Kit Barcode |
| 倒数日通知不准 | 复用已验证的AlarmManager方案 |

---

## 十七、与现有方案的关系

> 本文档为自律工作台V1.5的**8个独立工具模块规格**，主文档见 `自律工作台_完整方案_V1.5.md`。  
> 8个工具**不再聚合为"工具箱"**，而是作为**8个独立模块**与任务/笔记/运动等平级存在。  
> 每个模块可单独开关、单独排序、独立开发部署。  
> 所有数据走统一 `BaseEntity` 同步机制，不破坏现有架构。
